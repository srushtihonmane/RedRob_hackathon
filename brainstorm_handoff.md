# Redrob Ranking Challenge — Brainstorm Handoff

> **How to use this file.** We're brainstorming each goal in its own chat. In any goal chat, paste **PART A (Shared Context)** first, then paste the **one PART B goal section** you want to work on. PART A gives a cold chat everything it needs; PART B says where that goal stands and what's still open.

---

# PART A — Shared Context (paste in every goal chat)

## The task
Redrob "Intelligent Candidate Discovery & Ranking Challenge." Given a **single fixed job description** and a pool of **100,000 candidates** (`candidates.jsonl`, ~487 MB), produce a CSV ranking the **top 100 best-fit candidates**, with `candidate_id, rank, score, reasoning`. Scored once against a hidden ground truth after submissions close.

## Why this JD is special
The JD is the *winners' actual job*. Its "What you'd actually be doing" section tells you the company's real stack and objective, and your ranker should mirror it:
- Current system = **BM25 + rule-based scoring** ("working but not great") → your baseline.
- v2 they want = **embeddings + hybrid retrieval + (LLM) re-ranking** → your architecture.
- Eval = **offline benchmarks + A/B + recruiter-feedback loops**.
- The optimized metric = **recruiter-engagement / hireability**, NOT raw skill-match. So the behavioral signals are part of *relevance*, not just a fraud filter.

## What the JD actually wants (the typed criteria model)
- **Must-haves (hard, high weight):** production embeddings-retrieval (handled drift/index-refresh/regression); vector-DB or hybrid-search ops (Pinecone/FAISS/OpenSearch/etc.); strong Python; ranking eval frameworks (NDCG/MRR/MAP, offline↔online, A/B); **shipped an end-to-end ranking/search/rec system at a *product* company, at scale** (the spine).
- **Nice-to-haves (low weight):** LLM fine-tuning (LoRA/QLoRA/PEFT); learning-to-rank (XGBoost/neural); HR-tech; distributed systems; OSS.
- **Disqualifiers / strong negatives:** pure research, no production; "AI" = <12 mo LangChain+OpenAI with no pre-LLM ML; senior who hasn't coded in 18 mo ("moved to architecture"); title-chaser (~1.5-yr hops for title); career *entirely* at consulting firms (TCS/Infosys/Wipro/Accenture/Cognizant/Capgemini); CV/speech/robotics primary with no NLP/IR; 5+ yrs closed-source only with no external validation.
- **Context modifiers:** experience 5–9 yrs is a **soft band** (ideal 6–8), not a gate; location Noida/Pune or willing to relocate from a Tier-1 Indian city; notice period (≤30 ideal); **availability** (active, responsive, open_to_work).
- **Ideal profile:** 6–8 yrs total, 4–5 in applied ML at product (not services) companies; shipped a ranking/search/rec system at scale; has defensible opinions on retrieval/eval/LLM; in or willing to relocate to Noida/Pune; active on the platform.

## Candidate data shape (`candidate_schema.json`)
- `profile`: headline, summary, location, country, years_of_experience, current_title, current_company, current_company_size, current_industry.
- `career_history[]`: company, title, start_date, end_date, duration_months, is_current, industry, company_size, description.
- `education[]`: institution, degree, field_of_study, start_year, end_year, grade, **tier** (tier_1..tier_4/unknown).
- `skills[]`: name, **proficiency** (beginner/intermediate/advanced/expert), **endorsements**, **duration_months**.
- `certifications[]`, `languages[]`.
- **`redrob_signals` (23 behavioral signals):** profile_completeness_score, signup_date, last_active_date, open_to_work_flag, profile_views_received_30d, applications_submitted_30d, recruiter_response_rate, avg_response_time_hours, **skill_assessment_scores** (dict skill→0-100), connection_count, endorsements_received, notice_period_days, expected_salary_range_inr_lpa{min,max}, preferred_work_mode, willing_to_relocate, github_activity_score (-1 if none), search_appearance_30d, saved_by_recruiters_30d, interview_completion_rate, offer_acceptance_rate (-1 if none), verified_email, verified_phone, linkedin_connected.

## The traps (the contest is decided here)
1. **Keyword stuffers** — list every AI buzzword; wrong title/no corroboration. (The provided `sample_submission.csv` falls for this: it ranks an HR Manager #1 on "AI skills count.")
2. **Plain-language Tier-5s** — never write "RAG"/"Pinecone" but career history shows they built the system. Must **not** be penalized for missing buzzwords.
3. **Behavioral twins** — near-duplicate profiles/signal envelopes.
4. **Honeypots (~80, "subtly impossible")** — e.g. 8 yrs at a company founded 3 yrs ago; "expert" in 10 skills with 0 months used. Forced to relevance tier 0 in ground truth. **Honeypot rate >10% in your top 100 = disqualification (Stage 3).**

## Hard constraints & facts
- **Scoring:** `Final = 0.50·NDCG@10 + 0.30·NDCG@50 + 0.15·MAP + 0.05·P@10`. P@10 "relevant" = tier 3+. **Half the score is the top 10.**
- **Compute (ranking step only):** ≤5 min wall-clock, ≤16 GB RAM, **CPU only**, **no network**, ≤5 GB disk.
- **Precompute is ALLOWED (resolved from spec §10.3):** embeddings/indexes/weights may be built offline (may exceed 5 min) and shipped as artifacts; the *ranking step* must load—not recompute—them and finish in budget. Ship the precompute script too (Stage 4 checks git authenticity). Artifact must cover exactly the released pool.
- **No labels, no live leaderboard, 3 submissions max** (last valid counts). You must be your own offline benchmark.
- **Pipeline stages:** 1 format-validation → 2 scoring → 3 code reproduction + honeypot check → 4 manual review (reasoning quality, git authenticity, code quality) → 5 defend-your-work interview. AI tools allowed but AI-only fails at 3–5.

## Cross-cutting principles
1. **JD is singular & offline** → understanding it can be arbitrarily heavy, done once.
2. **Only per-candidate work is budget-constrained** → push everything possible into the offline phase.
3. **Pure similarity loses; pure rules miss plain-language fits** → the design rewards **learned matching + explicit logic/consistency checks layered together**. That layering is the "real engineering" being scored at Stages 3–5.
4. **Never trust one field** → require ≥2 corroborating sources; catch honeypots by *contradiction*.
5. **Defensibility counts** (Stage 5) → prefer transparent, explainable components where accuracy is comparable.

## The decided overall architecture
**Offline phase (unbudgeted):** stream `candidates.jsonl` once → build structured features + the 23 signals + sparse lexical doc + **dense embeddings** → persist as compact Arrow/Parquet + `.npy`/FAISS index. Encode the JD once into a 3-channel query (lexical terms / dense "ideal-candidate" embedding / structured gates+weights).
**In-budget ranking step:** load artifacts → **hybrid retrieve** (BM25 ∪ dense kNN) → **gate** (honeypot/consistency + hard disqualifiers floor candidates) → **re-rank** (fused, calibrated score with engagement multiplier) → **constrained top-100** (enforce honeypot rate ≪10%) → **generate reasoning** from the per-criterion evidence trail.

---

# PART B — Per-goal plans

### Goal 1 — Data ingestion — ✅ DECIDED (detailing in progress)
**SCOPE (rescoped, tight):** Goal 1 is **pure ingestion** — parse the 100k JSONL **once** and persist a faithful, fast-loading copy so nothing downstream re-parses the 487 MB JSON. **No** filtering, classification, derived/interpretive features, scoring decisions, hot/cold column split, or embeddings. *Embeddings (`.npy`, float32 L2-normalized, brute-force cosine, no-FAISS), the lexical/BM25 doc, derived features, and the numeric feature matrix all moved to **Goal 3** (candidate representation).* (Rejected for ingestion: load-all-as-dicts — RAM risk.)

**D1 — Storage layout (LOCKED): single denormalized nested Parquet table, one row per candidate.**
- Nested arrays (`career_history`, `education`, `skills`, `certifications`, `languages`) stored as native Arrow/Parquet **`list<struct>`**; `skill_assessment_scores` stored as native **`map<string, value>`** (not dynamic columns, not JSON).
- This table is the **single source of truth** and must support **lossless reconstruction** of the original record **without joins or JSON parsing**.
- Optional **projection tables** (`skills_flat.parquet`, `career_history_flat.parquet`, …) may later be derived for cross-candidate analytics — **read-optimized views only, never authoritative**.
- Priorities: fast candidate-level loading, schema fidelity, minimal downstream reconstruction overhead, with a retained path to future analytical workloads.
- *Rejected:* normalized star schema (joins, no payoff — downstream access is row-wise per candidate); flat-scalars-plus-JSON-strings (re-incurs JSON parsing we're killing).

**D2 — Schema fidelity & type handling (LOCKED):**
- **Dates** → native `date32` (consistent ISO `YYYY-MM-DD`, lossless round-trip); `end_date` nullable preserved as true null; education `start_year`/`end_year` → `int16`; parse failures fall to the D5 quarantine path rather than being guessed.
- **`-1` sentinels** (`github_activity_score`, `offer_acceptance_rate`) → **preserved verbatim**, never converted to null (the "−1 means missing" semantics belong to Goal 3).
- **Optional/missing arrays** (`certifications`, `languages`, `education`) → **normalize absent → empty `list`** (schema `minItems:0` treats absent ≡ empty; the one documented normalization).
- **Low-cardinality enums** (`company_size`, `proficiency`, `tier`, `preferred_work_mode`, `industry`) → **dictionary-encoded** (transparent storage win, identical round-trip).
- **By type:** `years_of_experience`→`float32`, `duration_months`→`int16`, counts→`int32`, rates→`float32`, booleans→native `bool`, `skill_assessment_scores`→`map<string,float32>`, free text→`string`.

**D3 — Streaming write mechanics (LOCKED):**
- **Loop:** stream JSONL line-by-line → buffer parsed records → flush each buffer as one Arrow `RecordBatch` via an open `pyarrow.parquet.ParquetWriter` (only one batch resident at a time). *Rejected:* build one giant Arrow Table then write once — RAM spike with nested `list<struct>`.
- **Batch / row-group size:** **~5,000–10,000 candidates per batch** (~25–50 MB resident; ~10–20 row groups total).
- **Compression:** **Zstd (~level 3)** — better ratio than Snappy, decode cost irrelevant vs budget, compresses bulky text well.
- **Schema:** **explicit pinned Arrow schema** (per D2) constructed for every batch — never infer from data (avoids cross-batch type drift, e.g. all-null `end_date` or empty `certifications` in the first batch; more defensible at Stage 3).

**D4 — Canonical ordering & ID contract (LOCKED):**
- **Ordering:** **preserve source order** — row *i* = the *i*-th line of `candidates.jsonl` (deterministic, no sort pass, keeps the pure-streaming write). *Rejected:* sort-by-`candidate_id` (adds a full materialization/sort, no payoff — no id-locality relied on).
- **ID contract:** `candidate_id` is a column in the canonical table in row order = the authoritative index; all Goal 3 artifacts are built by iterating this table in order (alignment guaranteed by construction). Also emit a standalone **`candidate_ids`** key (`.npy` int32 of the 7-digit part / small parquet) for cheap alignment checks without loading the nested table.
- **Verification:** carry a `row_index` column; every downstream artifact derives from the same `candidate_ids` array; startup **assertion** that lengths match and ids line up (silent misalignment → loud failure).

**D5 — Integrity & reproducibility (LOCKED):**
- **Completeness assertions (hard, abort on failure):** exactly **100,000 rows**; `candidate_id` unique and matching `^CAND_[0-9]{7}$`; required fields present. No silent partial artifacts.
- **Malformed-record policy:** **quarantine, don't drop, don't crash.** Failing lines (with line number + error) → `quarantine.jsonl` + count in manifest. Policy gate: 0 quarantined → proceed; >0 → surface loudly, require human decision before artifact is valid. Expected 0 for this released synthetic pool.
- **Determinism:** explicit pinned Arrow schema (D3) + fixed source order (D4) + pinned lib versions (`pyarrow`, Python) in `requirements.txt` + a fixed reference-date constant for downstream date math; document the single regenerate command (mirrors Stage-3 reproduction).
- **Manifest:** emit `ingest_manifest.json` — row count, quarantine count, source filename + SHA-256, output SHA-256, schema fingerprint, lib versions, timestamp, git commit (Stage 3/4 artifact-authenticity evidence).

**STATUS: Goal 1 fully detailed & locked (D1–D5).** Output artifacts: `candidates.parquet` (canonical nested table), `candidate_ids` (alignment key), `ingest_manifest.json`, optional `quarantine.jsonl`. Next: Goal 2 (JD understanding).
**[Goal 8 addendum — LOCKED, verified non-breaking]:** the per-record **parse/normalize logic is factored as a reusable pure function** `parse_record(raw)→canonical struct`, **distinct from** the offline streaming Parquet-writer (D3), so the **Sandbox Runtime** (Goal 8 D8) can parse a ≤100-candidate sample straight into memory without the Parquet path. The builder boundary is the *record parser*, not the writer. Pool-level integrity (exactly-100k assertion, quarantine policy, ingest manifest — D5) stays in the **offline driver only** (it is meaningless on an arbitrary sample). No conflict with D1–D5: the streaming write still composes the same `parse_record` over the full file.

### Goal 2 — JD understanding — ✅ FULLY DETAILED & LOCKED (D1–D8)
**SCOPE:** process the single fixed JD once, offline, heavy. Emit a **3-channel JD query** persisted as a frozen artifact: (a) curated lexical concept terms+synonyms (BM25 layer), (b) dense "ideal candidate" embedding (semantic layer), (c) structured **typed-criteria** object (rule layer). Goal 2 declares **what to look for + target semantics + which fields hold the evidence**; it does **not** assign numeric weights, thresholds, scoring formulas, or detection logic (those live in Goals 4/5). Objective is **engageable-hireability**, so behavioral signals feed relevance, not just fraud filtering.

**Data-profiling findings that shaped these decisions (all 100k profiled):**
- **Skills arrays are deliberately diluted noise** — every skill name appears ~12,000× (near-uniform). Matching `skills[].name` is worthless = the engineered keyword-stuffer trap. **Lexical channel must not lean on the skills array.**
- **Real concepts are rare and live in narrative text:** "ranking/search/rec" language in only **1.0%** of profiles, embeddings/vector **0.2%**, eval/NDCG/MRR **0.1%**; target titles (ML/AI/search/rec engineer) **0.8%**. (LLM/NLP language 51% = the "AI = recent LangChain" inflation.) → dense channel does the heavy lifting for plain-language Tier-5s.
- **`skill_assessment_scores` is the corroboration goldmine but sparse** (only 24% have any) and skews CV/speech (YOLO, CNN, OpenCV, ASR, TTS, diffusion) — that population *is* the "CV/speech primary, no NLP/IR" disqualifier; IR/PEFT/Milvus assessments are rare (~340 each) = genuine signal.
- **Geography:** 75% India, 8.5% Noida/Pune, 37.6% Tier-1 IN city → soft modifier, not a gate. **Consulting:** 9.7% career-entirely-consulting (disqualifier pop.); 61.5% have *some* consulting stint (must NOT disqualify). **YOE 5–9 = 34%, 6–8 = 18%** → soft band confirmed.
- **Engagement is the live differentiator:** last-active 32–272 days (median 137), GitHub absent 64.6%, offer-history absent 59.6%, open_to_work 35%. **Honeypots detectable:** 84 skills show expert+0-months; 25 candidates have YOE exceeding summed tenure by >5yr.
- **Takeaway:** corroboration across narrative + title + assessment + signals is everything; any single field (esp. skills) is a trap → corroboration is first-class in the schema.

**D1 — Typed-criteria schema (LOCKED): typed criteria with evidence binding (declarative *what*, not *how*).** Per-criterion fields: `id`, `criterion_type` ∈ {`hard_gate`, `strong_negative`, `must_have`, `nice_to_have`, `context_modifier`}, `polarity` (+/−), `importance` ∈ {`critical`,`high`,`medium`,`low`} (numeric weights deferred to Goal 4), `description`, `evidence_sources[]` (explicit candidate-schema field paths), `corroboration{min_sources}` (criterion-level), `match_modes[]` ⊆ {`lexical`,`semantic`,`structured`,`signal`}, `target_kind` ∈ {`presence`,`range`,`gate`,`modifier`}, optional `override_condition`, and `provenance` (JD origin, explicit or implied). *Excluded from Goal 2:* numeric weights, similarity thresholds, scoring formulas, executable predicates, detection/retrieval logic.

**D2 — Criteria catalog (LOCKED, structure):**
- **Must-haves** (positive, `presence`, kept as **5 separate** criteria for traceability; mostly lexical+semantic, `min_sources:2`): `production_embeddings_retrieval` (CRITICAL), `vector_db_hybrid_search_ops` (CRITICAL), `shipped_ranking_search_rec_at_scale` (CRITICAL; +`structured` product-not-services & company_size), `ranking_eval_frameworks` (HIGH), `strong_python` (HIGH; lean on `skill_assessment_scores`, not the diluted skills array).
- **Nice-to-haves** (positive, `presence`, LOW, `min_sources:1`): `llm_finetuning`, `learning_to_rank`, `hr_tech_marketplace`, `distributed_systems_scale`, `open_source_contributions`.
- **Disqualifiers** (negative, two-tier gates per D7): `pure_research_no_production` (hard_gate), `consulting_entire_career` (hard_gate, fires only when no product stint), `recent_llm_only_no_preml` (strong_negative + override), `senior_not_coding_18mo` (strong_negative), `title_chaser_1p5yr_hops` (strong_negative), `cv_speech_robotics_no_nlp` (strong_negative), `closedsource_5yr_no_external_validation` (strong_negative).
- **Context modifiers** (`range`/`modifier`): `experience_band` (range, 5–9 soft / 6–8 ideal), `applied_ml_years_at_product` (range, ideal 4–5), `location_noida_pune_relocate` (modifier), `notice_period` (modifier, ≤30 ideal), plus an **availability/engagement family** modeled as **atomic per-signal criteria** (`match_modes:[signal]`): open_to_work, last_active recency, recruiter_response_rate, saved_by_recruiters, interview_completion, verifications — transparent, lets Goal 6 build the engagement multiplier from explicit parts.
- **Consistency:** one umbrella `profile_internally_consistent` **hard_gate** (negative); exact honeypot detection deferred to Goal 5, but the criterion lives in the artifact so reasoning can cite it.
- **Omitted:** "between the lines" traits (writes-a-lot/async, defensible-opinions, plans-to-stay-3yr) — undetectable on templated ~460-char synthetic summaries; "plans to stay" already inverse of `title_chaser`.

**D3 — Authoring & verification (LOCKED): static, LLM-assisted, frozen, version-controlled artifact treated as source code.** Human-reviewed before commit; **no build-time or runtime LLM/network dependency** (reproduction = load the file). LLM allowed only during authoring (brainstorm/draft/surface implied criteria); final artifact human-approved. **Build-time validation:** well-formed schema, valid criterion_types/target_kinds/match_modes, unique IDs, every `evidence_sources` path exists in `candidate_schema.json`. **Per-criterion provenance** prevents criterion drift. Boundary confirmed: Goal 2 = declarative criteria; Goal 3 = candidate features, Goal 4 = scoring/fusion/weights, Goal 5 = detection rules.

**D4 — Lexical channel (LOCKED): curated discriminative concept groups, IDF-suppressed.** Concept groups (retrieval, vector-infra, ranking-eval, LLM, LTR, …) + **conservative** synonym expansion (canonical terms + well-known abbreviations/surface forms only — e.g. recommender/rec-sys, vector db/vectordb, FAISS, NDCG; **no loose paraphrases** — those are the dense channel's job). **Additive-positive only** (absence never penalizes → protects plain-language Tier-5s). Rely on **BM25 IDF** to auto-crush the diluted skill tokens. Candidate-side BM25 doc (built in Goal 3) draws from narrative text, **not** the skills array. Per-group boost magnitudes deferred to Goal 4.

**D5 — Dense channel (LOCKED): synthesized "ideal candidate" narrative → single positive vector.** Author a positive ideal-candidate narrative in the same register as a strong candidate's summary+career (positive signal only — **not** the raw JD, which is noisy/negative-polluted); embed it as **one** vector (mean of 2–3 paraphrasings allowed for robustness). **Model: `bge-small-en-v1.5` (384-d), L2-normalized, with a query-prefix convention — LOCKED for BOTH JD and candidate sides** (this resolves Goal 3's embedding-model open item; the same prefix discipline must be applied candidate-side in Goal 3).

**D6 — Anti-profile / negative anchor (LOCKED): per-archetype anti-vectors.** Author short negative narratives for disqualifier archetypes (research-only, CV/speech/robotics, consulting-only, recent-LLM-only, keyword-stuffer) and embed each → per-candidate cosine to each archetype is a feature (explainable: "resembles CV/speech archetype"). The artifact also **declares the divergence inputs** for the stuffer detector: `lexical_score − dense_cosine(ideal)`. Penalty/threshold formulas deferred to Goal 4/5.

**D7 — Gate semantics & importance (LOCKED): two-tier gates + override clauses.** `hard_gate` = absolute floor → tier 0 / excluded (internal-consistency, pure-research-no-production, consulting-entire-career); `strong_negative` = heavy, **overridable** penalty (recent-LLM-only, senior-not-coding, title-chaser, CV/speech, closed-source). Mirrors the JD's own "we will not" vs "we will probably not" wording. `override_condition` field encodes the JD's "unless" clauses declaratively (e.g. recent-LLM-only overridden by pre-LLM-ML evidence; consulting fires only with no product stint); detection still deferred to Goal 5. Importance levels set here; **importance→numeric-weight mapping owned by Goal 4.**

**D8 — Output artifact & reproducibility (LOCKED): vectors derived, text canonical.** Bundle: **`jd_query.json`** (canonical, human-readable: `schema_version`; `model{name:bge-small-en-v1.5, dim:384, normalize:L2, query_prefix}`; typed criteria with provenance; lexical concept groups+synonyms; the ideal-candidate narrative **text**; the anti-profile archetype narratives **text** + ordered labels; divergence-input declaration; references to vector files), **`jd_ideal.npy`** `[1×384]` f32 L2-normalized, **`jd_antiprofile.npy`** `[A×384]` f32 L2-normalized (row order = archetype labels), **`jd_manifest.json`** (sha256 of each file, model name+revision, dim, normalization, prefix, git commit, timestamp, schema fingerprint, build-validation status — mirrors Goal 1 manifest discipline), optional **`jd_rationale.md`** (Stage-5 defense). `.npy` vectors are regenerable from the canonical narratives + pinned model.

**STATUS: Goal 2 fully detailed & locked (D1–D8).** Output artifacts: `jd_query.json`, `jd_ideal.npy`, `jd_antiprofile.npy`, `jd_manifest.json`, optional `jd_rationale.md`. **Not yet built** — next is to author the actual `jd_query.json` (full enumerated catalog + ideal/anti narratives + lexical groups) and generate the vectors.
**Deferred out of Goal 2 (by design):** per-criterion numeric weights + importance→weight mapping (Goal 4); divergence/anti-profile penalty formula + similarity thresholds (Goal 4/5); exact honeypot/consistency detection rules (Goal 5).

### Goal 3 — Candidate representation — ✅ FULLY DETAILED & LOCKED (D1–D9)
**SCOPE:** offline, iterate Goal 1's canonical `candidates.parquet` in row order → emit the candidate-side counterpart to Goal 2's 3-channel JD query: (1) **dense embeddings** (identity + evidence vectors), (2) **lexical/BM25 index**, (3) **structured feature matrix** (derived features + 23 signals + corroboration channels). **Governing principle (set by D4): expose inputs, not verdicts** — Goal 3 stores raw/continuous derived values, deterministic flags, and pool statistics; ALL thresholds, weights, transforms, gate-firing, corroboration counting, and composites defer to Goals 4/5/6. Embedding model locked by Goal 2 D5 (`bge-small-en-v1.5`, 384-d, L2-norm, query/passage prefix; candidate side uses *passage* prefix).

**Data-profiling findings that shaped Goal 3 (all 100k):**
- Text overflows the 512-token window for the top ~quartile: summary ~463 chars median (max 999), each role desc ~400 chars, median 3 roles (p95 6, max 9); concat median ~1.7k chars (~370 tok), p95 ~2.9k (~620 tok), max 4.6k (~980 tok). → composition/truncation matters; cannot naively embed everything.
- **Only 63 distinct companies**, each with a perfectly stable industry → product-vs-services is a deterministic curated lookup, not inference. Consulting (Infosys/Wipro/TCS/Capgemini/HCL/Mindtree/Accenture/Cognizant/Tech Mahindra/Mphasis) vs real product (Swiggy/Razorpay/CRED/Zomato/Flipkart/Meesho/Ola/Zoho…) vs fictional filler (Pied Piper/Initech/Hooli/Wayne/Acme/Stark/Globex/Dunder Mifflin).
- Title-escalation is absent (staff 13×, lead 11×, principal 0×) → title-chaser must be **tenure/hop-based**, not title-token-based.
- `skill_assessment_scores`: 24.2% coverage; CV/speech assessments common (~1.1k each), IR/PEFT/vector rare (~340) = high-signal. Corroboration gold + cv_speech negative evidence.
- Consistency tails real: 25 candidates YOE−tenure >5yr, 23 tenure≫YOE, 21 expert/advanced+0-month. Exactly 1 current role each.
- Signals heavily missing: github −1 64.6%, offer −1 59.6%, open_to_work 35%, relocate 29%, verified_email 72%; last_active 32–272 days (median 137, nobody fresh → recency is relative). Ph.D ~17.5k×; ~73% fields CS/IT/DS/ML/AI-relevant.

**D1 — Artifact set & formats (LOCKED).** Emit: `embeddings_identity.npy` & `embeddings_evidence.npy` (`[N×384]` f32 L2-norm), `features.npy` (`[N×F]` f32, runtime scorer artifact), `features.parquet` (named-column inspection/debug, native nulls), `feature_manifest.json` (rich schema/provenance/groups/logical dtypes/derivation versions), `bm25_index/` (D3), `candidate_ids.npy`, `normalization_stats.json` (D7), `repr_manifest.json`. Dense-`.npy`+rich-manifest keeps runtime simplicity while preserving observability/reproducibility. Raw text for reasoning citations read on demand from Goal 1's parquet, not duplicated.

**D2 — Dense-embedding text composition (LOCKED): two vectors per candidate.**
- **Identity embedding** = `current_title` + `headline` + `summary` (what the candidate presents as; captures title signal, current positioning, pivots, stuffer behavior). One vector, passage prefix, L2-norm.
- **Evidence embedding** = what they have actually done. Build a per-role doc (title+description), embed each role offline, compute `role_relevance = cosine(role_emb, jd_ideal)` (Goal 2 frozen ideal). **Always keep all role titles** (high-signal, low-token); spend the remaining ~480-token budget on the **highest-relevance** role descriptions (query-aware, **not** chrono). Assemble one evidence doc (all titles + top-relevance descriptions) → one final evidence embedding.
- *Rejected:* chronological truncation; pure per-role embeddings. *Free byproducts → features:* role-relevance summaries (G9), `identity_evidence_divergence` (G10) as an orthogonal stuffer/pivot signal. Query-aware selection is legal (singular fixed offline JD) and makes the evidence cosine meaningful **comparatively** — Goal 4 owns absolute scale/calibration. Deterministic assembly order: titles chronological, then descriptions relevance-descending with candidate_id/role-index tie-break.

**D3 — BM25/lexical doc (LOCKED): narrative-only corpus + prebuilt index.**
- Doc fields: headline + summary + all role titles (≈2× token multiplicity, light weighting) + all role descriptions. **Skills array excluded** (uniform ~12k× noise). Transparent BM25F-via-multiplicity, not field-structured BM25F.
- Tokenization: lowercase alphanumeric unigrams + **controlled phrase-joining** from Goal 2's lexical concept vocabulary (preserves "information retrieval", "vector database", "learning to rank", "sentence transformers" as single tokens, both corpus & query side).
- Ship prebuilt offline: `bm25_index/` = CSR doc-term `.npz` + `idf.npy` + `doclen.npy` + `vocab.json`; runtime scores fixed JD query terms via one sparse mat-vec. Corpus = full 100k. Additive-positive (absence never penalizes) enforced at Goal 4 scoring, not in the artifact.

**D4 — Derived structured feature catalog (LOCKED). Principle: expose inputs, not verdicts.** Ten groups:
- **G1 Experience/tenure:** yoe, summed_tenure_yrs, n_roles, current_role_tenure_mo, longest_role_tenure_mo, median/mean_role_tenure_mo.
- **G2 Job-hop/title-chaser (tenure-based):** count_stints_under_18mo, frac_short_stints, switches_per_year, mean_tenure_at_exit. (No seniority-escalation feature — data lacks it.)
- **G3 Company type (curated 4-way lookup `company_table.json`):** per-stint tag ∈ {product_tech, services_consulting, nontech, fictional}; candidate-level any_product_stint, frac_career_services, is_consulting_entire_career, product_tenure_yrs, max_company_size_at_product, **largest_employer_fraction** (frac of career tenure at largest employer).
- **G4 Applied-ML-at-product (components, NOT a final verdict):** product_tenure_yrs, weighted_ml_role_tenure_yrs, role-relevance summaries + per-role inputs (is_product, role_relevance, tenure). Goal 4 combines.
- **G5 Domain affinity (anti-profile cosines, BOTH vectors):** `identity_cos_{archetype}` AND `evidence_cos_{archetype}` for every Goal 2 anti-profile (research-only, cv_speech_robotics, consulting-only, recent-llm-only, keyword-stuffer); + cv_speech_vs_nlp_lean. Not collapsed to a single cosine.
- **G6 Education:** highest_tier_ordinal, has_tier1, highest_degree_level, has_phd, field_relevance, latest_grad_year. (Grades/CGPA ignored.)
- **G7 Consistency/honeypot inputs (internal only; Goal 5 owns thresholds):** yoe_minus_summed_tenure, expert_skill_zero_duration_count, skill_duration_exceeds_career_flag, role_duration_vs_datespan_mismatch_count, timeline_overlap_months, gradyear_vs_yoe_gap. **No company-founding table** (optional future Goal 5 experiment only).
- **G8 Location:** is_noida_pune, is_tier1_indian_city, is_india, country.
- **G9 Role-relevance summary (free from D2):** max/mean/recent role_relevance.
- **G10 Precomputable divergence:** identity_evidence_divergence. (Lexical-vs-dense divergence assembled at Goal 4 runtime.)

**D5 — 23-signal encoding (LOCKED): atomic, raw.** Pass-through raw numerics (completeness, views, applications, recruiter_response_rate, avg_response_time_hours, connection_count, endorsements_received, notice_period_days, search_appearance_30d, saved_by_recruiters_30d, interview_completion_rate). Sentinel→value+present flag (github_activity_score+github_present; offer_acceptance_rate+offer_history_present; never missing→0). Booleans→0/1 (open_to_work, willing_to_relocate, verified_email, verified_phone, linkedin_connected). preferred_work_mode→one-hot×4 + preferred_work_mode_count. Salary: expected_salary_present, _min, _max, _mid. Date-derived: last_active_recency_days, account_tenure_days. **REF_DATE = max(last_active_date in pool) + 1 day**, pinned in manifest (never build/runtime/today()). Manifest role tags: availability/engagement/intent/credibility. No normalization/composites here.

**D6 — Corroboration representation (LOCKED): source-level inputs only.** Shared concept registry = Goal 2 concept groups (retrieval_embeddings, vector_db, ranking_eval, learning_to_rank, strong_python, llm_finetune, distributed_systems, cv_speech, …). Per concept: **assessment** (primary, high-trust) `assess_{c}_max`, `assess_{c}_present`; **skillmeta** (secondary, low-trust, metadata only — never presence) `skillmeta_{c}_max_duration`, `skillmeta_{c}_max_endorsements`; **title** `title_hit_{c}`, `title_hit_count_{c}`. Evidence-source abstraction = {assessment, title, skillmeta} static (Goal 3) + {dense, lexical} runtime (Goal 4). **Goal 3 never computes corroboration counts**; Goal 4 owns min_sources, cross-source agreement, criterion satisfaction. Core rule: a single self-reported field never satisfies corroboration.

**D7 — Missing-data & normalization (LOCKED).** Missing = **NaN + companion `_present` flag**; invariant **`_present==0 ⇒ value MUST be NaN`**; no sentinels, no imputation. Career/profile-derived features non-nullable. Runtime masks via flags / nan-ops (never arithmetic on raw NaN). **Normalization deferred:** ship raw `features.npy` + `normalization_stats.json` per feature {count, missing_count, min, max, mean, std, p1, p5, p25, p50, p75, p95, p99}; Goal 4 picks & applies transforms (quantiles enable rank-norm of skewed counts). No pre-normalized matrix. Manifest `suggested_scale` advisory only.

**D8 — Alignment, manifest & reproducibility (LOCKED).** Row i aligns across features.npy/parquet, both embedding files, candidate_ids.npy, and Goal 1's parquet; startup assertion N=100k + candidate_ids match (loud fail). `feature_manifest.json`: schema_version, REF_DATE, per-column (index, name, group, logical dtype, source paths, derivation desc+version, nullable+present-flag, signal role, corroboration concept+source, advisory suggested_scale) + concept-registry & company_table references + anti-profile archetype label order (= jd_antiprofile.npy row order). `repr_manifest.json`: sha256 of all Goal-3 artifacts + upstream (Goal 1 parquet; Goal 2 jd_ideal/jd_antiprofile/jd_query) + **concept_registry & company_table hashes** + model name+revision, lib versions, git commit, timestamp, F, schema fingerprint, build-validation status. Build-time validation: F matches manifest; NaN pattern matches present-flags; no undeclared all-NaN column; archetype order & concept registry match Goal 2; candidate_ids match Goal 1.

**D9 — Build mechanics (LOCKED).** Iterate Goal 1 parquet in row-group order (~5–10k/batch); never re-parse the 487 MB JSON. Passes: (1) pre-scan REF_DATE; (2) main build — assemble identity + per-role docs, batch-embed CPU (passage prefix), compute role_relevance, assemble query-aware evidence doc + embed, archetype cosines, derive G1–G10 + signals + corroboration; write features.parquet incrementally + accumulate features.npy + memmap'd embeddings; (3) normalization_stats in-memory (N×F ≈ 50 MB). **Retain audit outputs `role_relevance_raw`, `role_rank_within_candidate`** during build. ~(2+n_roles) embeddings/candidate (~500k docs), offline/unbudgeted, batched (~256), eval mode. Note minor BLAS float nondeterminism in embeddings — cosine ranking robust; pin model revision + libs. 16 GB cap is runtime-only; offline memory comfortable. Atomic writes, manifest last; single regenerate command mirrors Stage-3 reproduction.

**STATUS: Goal 3 fully detailed & locked (D1–D9).** Output artifacts: `embeddings_identity.npy`, `embeddings_evidence.npy`, `features.npy`, `features.parquet`, `feature_manifest.json`, `bm25_index/`, `candidate_ids.npy`, `normalization_stats.json`, `repr_manifest.json`. **Not yet built.** Next: Goal 4 (re-ranker fork + fusion/calibration).
**[Goal 7 addendum — LOCKED]:** also emit **`evidence_snippets`** — per-candidate, **criterion-linked narrative spans** (best-matching summary/career snippets per concept) with **stable `evidence_id`s**, extracted during the D9 build. This **overrides D1's "raw text read on demand"** *for citation purposes*: a curated *snippet* sidecar (NOT full-text duplication) so runtime never reopens Goal 1's parquet. These `evidence_id`s are the stable handles Goal 4's evidence trail and Goal 7's renderer reference.
**[Goal 8 addendum — LOCKED, verified non-breaking]:** (1) the **`evidence_snippets` sidecar must be a row-addressable format** (offsets-index + blob, or Arrow row-access) supporting **O(100) random access** at runtime (Goal 8 D5) — the runtime materializes only the top-100's snippets, never a full load. Consistent with the Goal-7 addendum (already a curated, non-full-text sidecar); only the storage layout is newly constrained. (2) The per-candidate **feature/embedding/snippet derivation (D2–D6, D9 passes 2) is factored as reusable `builder(candidate, frozen_inputs)` functions** taking **REF_DATE, jd_ideal/jd_antiprofile, company_table, concept_registry, model** as **frozen parameters**, so the **Sandbox Runtime** (Goal 8 D8) can derive *identical raw features* for a ≤100 sample. The sandbox **skips the D9 pass-1 REF_DATE pre-scan** and uses the **frozen pool REF_DATE** (per Goal 8 D8 frozen-stats rule). Verified pool-independent given frozen inputs: D7 already exposes **raw** features (normalization deferred to Goal 4), so the only pool-level quantity in Goal 3 is REF_DATE, which is frozen — no other recomputation leaks into the per-candidate path. No conflict with D1–D9.
**Goal 3 → Goal 4 carry-overs:** (1) features are raw + normalization_stats frozen → Goal 4 owns transforms. (2) Corroboration exposed as static sources {assessment, title, skillmeta}; Goal 4 adds {dense, lexical} and counts min_sources. (3) Two embeddings (identity, evidence) + identity_evidence_divergence + dual archetype cosines available; lexical-vs-dense divergence assembled at runtime. (4) G4 applied-ML components are inputs, not a verdict — Goal 4 combines. (5) NaN+_present contract: scorer must mask, never do arithmetic on NaN.

### Goal 4 — Candidate↔JD scoring — ✅ FULLY DETAILED & LOCKED (D1–D9, incl. D6.5)
**SCOPE:** Goal 4 = the **relevance estimate**. It owns retrieval fusion, feature transforms, criterion satisfaction, corroboration, dense/lexical/CE fusion, and emits a **fit bundle**: `fit_score`, component subscores, a per-criterion **satisfaction degree + evidence trail** (feeds Goal 7), a bounded `engagement_modifier`, and a bounded `risk_modifier` (with sub-components exposed). It does **not** make decisions. **Conceptual hierarchy (locked):** Goal 4 = relevance, Goal 5 = risk (hard trap/honeypot/disqualifier *detection*), Goal 6 = decision policy. Goal 6 computes `final_score = fit_score × engagement_modifier × risk_modifier` subject to `hard_gate==false ∧ honeypot==false`. **Soft risk shapes the score inside Goal 4; hard risk excludes inside Goal 6** — different mechanisms, no overlap.

**Data-profiling findings that shaped Goal 4 (all 100k, re-confirmed):**
- **Positives are vanishingly rare and Stage-A recall loss is fatal:** narrative signal — ranking/search/rec ~959 (1.0%), embeddings/vector 155 (0.2%), eval/NDCG 74 (0.07%); target titles ~0.8%. The pool is ~95% obviously-irrelevant roles (Business Analyst, HR Manager, Mechanical Engineer…, each ~5–6k). → recall-first retrieval; tiny positive class + **zero labels** → learned LTR is data-starved.
- **No labels, no leaderboard, 3 submissions** → we are our own ground truth; tuning is audit-driven, not metric-driven.
- **Half the score is NDCG@10** → margins at the very top matter (favors magnitude-preserving fusion over pure rank/RRF).
- **Assessment coverage only 24%** → assessments are high-trust *when present* but can never be *required* (would silently disqualify assessment-less true positives) → demonstrated career-evidence must be an independent satisfying source.
- **Cheap channels are nearly free** (cosine over 100k×384 ≈150 MB, BM25 mat-vec, vectorized features all sub-budget); the only "expensive" op is the cross-encoder, which is **precomputable offline** since the JD is fixed → the cascade exists to bound expensive precompute + mirror the JD's v2, not to save runtime.

**D1 — Boundary & output contract (LOCKED): decomposed fit bundle, decisions deferred to Goal 6.** Goal 4 emits relevance + evidence + the `engagement_modifier`/`risk_modifier` inputs; Goal 5 supplies hard flags; Goal 6 applies gates, the multipliers, and the honeypot-constrained top-100. Keeps the locked Goal 5/6/7 scopes intact and gives the eval harness clean, independently-ablatable knobs. *Rejected:* single finished score (A — tangles gating/selection into Goal 4, less defensible/ablatable); Goal-4-applies-gates (C — splits gating across goals, invites drift).

**D2 — Re-ranker model fork (LOCKED): transparent weighted scorer = primary & final relevance arbiter.** *Rejected as primary:* LightGBM/LambdaMART LTR — needs relevance labels we don't have; programmatic labels make it merely mimic our heuristic (circular, overfits noise), hand-labels give too few positives; black-box, harder to defend, latches onto honeypots. **Parked** as a future ablation only if D9 ever yields credible graded labels. **Cross-encoder reclassified** from alternative re-ranker → optional **precomputed relevance *feature*** (bounded positive contribution; may never override structured evidence, corroboration failures, disqualifiers, or trap logic). The scorer's weakness — **interaction blindness** — is acknowledged and recovered *explicitly* via D6/D7 (must-have soft-ANDs, corroboration requirements, critical-criterion combinations), never an opaque model.

**D3 — Retrieval cascade / shortlist (LOCKED): generous recall-first union.** Shortlist ≈ **5–8k unique** via `dense_evidence_topK ∪ dense_identity_topK ∪ BM25_topK ∪ structural_prefilter` (K≈2–3k/channel). **Membership = union, not fused ranking** (RRF/fused cutoff could drop a candidate strong in only one channel — exactly the Tier-5 strong on dense-evidence but weak on BM25; the real ranking is Stage B's job). **Structural prefilter** guarantees inclusion of strong non-text evidence (assessment corroboration, substantial product-company ML tenure) even when lexical/dense retrieval is weak. **No exclusionary gates in retrieval** — honeypots/disqualifiers may enter; detection = Goal 5, enforcement = Goal 6. Stage B (transparent scorer) + cross-encoder features run **only** on the shortlist. *Rejected:* tight ~1–2k shortlist (fatal recall risk for compute we don't need to save).

**D4 — Feature transforms & normalization (LOCKED): expose-raw → declarative transform spec.** Default for skewed continuous = **quantile/rank-norm** using Goal 3's frozen full-pool percentiles, saturating beyond p1/p99 (robust to planted outliers, distribution-free, no tuning). Already-bounded features (rates, flags, normalized similarities) → identity/simple rescale. **Non-monotonic band features (YOE, applied-ML years) excluded from generic normalization** → explicit membership functions in D6. All transforms standardized to **higher = better fit** (lower-is-better inverted post-transform). **Missing → neutral, never negative** via the Goal 3 `_present`-flag masking contract. Transforms fit on the **full 100k pool**, never the shortlist. A version-controlled spec maps each feature → `{transform, direction, params, neutral}`. Finer percentile grids deferred unless audits reveal a calibration problem (optimize for simplicity/stability/reproducibility first).

**D5 — Score fusion & calibration (LOCKED): hierarchical, criterion-satisfaction spine.** The **structured criterion-satisfaction score is the fit spine**; BM25/dense/CE act **primarily as evidence sources for criterion satisfaction** (D7), not independent additive channels (flat fusion *rejected* — a high dense cosine alone would inflate fit = the keyword-stuffer hole). On top, **dense-evidence + CE add small, capped, audit-tuned global bonuses** (Tier-5 safety net) that may refine but never dominate fit. **Relevance channels use tail-anchored calibration** against full-pool distributions (pin the irrelevant *bulk* ≈ p90–p95 → 0, reward only the upper tail → 1; preserves additive-positive *and* magnitude — quantile-norm rejected for relevance because it hands the irrelevant median 0.5). **Pool-level BM25/dense/CE distribution statistics emitted offline** for reproducible, shortlist-independent calibration.

**D6 — Importance→weights, must-have combination & band functions (LOCKED).** Importance→weights via a **fixed tiered ladder** by class (critical/high/medium/low → one number each; per-criterion override only where an audit demands; exact values audit-tuned). Must-haves combine via **weighted geometric-mean soft-AND** (any near-zero must-have drags the block down, but no single thin-evidence axis zeros a genuine fit; floor ε; *coverage-multiplier rejected* as more brittle). Nice-to-haves = **subordinate, capped, multiplicative** (`×(1+NH_bonus)`) so they differentiate only candidates who already clear the must-haves and can never compensate a must-have failure. Context modifiers (YOE, applied-ML years, location, notice) = **trapezoidal membership → bounded multiplicative nudge** (`CM_modifier`, clamped). Aggregation: `structured_criteria_score = MH_softAND × (1 + NH_bonus) × CM_modifier`. Multiplicative subordination is itself an interaction guard.

**D6.5 — Engagement modifier (LOCKED): bounded multiplier computed in Goal 4, applied in Goal 6.** Captures **availability + responsiveness only**. Core (high wt): `recruiter_response_rate`, `last_active_recency` (inverted, quantile — recency is relative), `open_to_work_flag` (soft positive, not a gate). Secondary: `interview_completion_rate`, `avg_response_time` (inverted), `applications_submitted_30d`. Market-demand (low, cautious): `saved_by_recruiters_30d`. **Credibility signals split out** (verifications, completeness, offer_acceptance → Goal 5 risk / separate nudge, not engagement). `engagement_modifier = clamp(1 + (E − E_median)·s, 0.7, 1.0→1.1)` — **asymmetric [0.7, 1.1]**, more downside than upside (a ghost loses ~30%; a star gains only ~10% → engagement can never manufacture a top pick). Missing → neutral (1.0). The **multiplicative-on-fit structure is the real defense**: low-fit × high-engagement stays low → kills the sample_submission "active but irrelevant HR-Manager" trap.

**D7 — Corroboration → criterion satisfaction (LOCKED): trust-tiered noisy-OR with a soft gate.** **Three trust tiers** by fakeability: Tier-1 **Verified** (`skill_assessment_scores`, `assess_{c}_max ≥ θ_assess`; high-trust but only 24% coverage → never required); Tier-2 **Demonstrated** (career-history evidence: dense-evidence cosine + career-*description* lexical, up-weighted when title/role/company consistent — the universal hard source protecting assessment-less Tier-5s); Tier-3 **Declared** (summary/headline lexical, title tokens, skills-array — the stuffer's toolkit). **Skillmeta = modulator only** (raises confidence of an existing hit, never creates one). **Strong satisfaction requires ≥1 Tier-1-or-2 source**; **Tier-3-only evidence is smoothly attenuated toward a small floor** (not a hard ceiling). Aggregation = **noisy-OR** of trust×strength contributions (`raw = 1 − Π(1−cᵢ)`), × a **soft corroboration gate** `smoothramp(n_independent/min_sources)` so thin evidence is depressed, not annihilated. **Validated on both adversarial cases:** stuffer → Tier-3 only, no Tier-1/2, `n<min_sources` → satisfaction ~floor → soft-AND collapses → low fit; plain-language Tier-5 → Tier-2 (±Tier-1) satisfies must-haves with zero buzzwords (no buzzword-absence penalty anywhere, additive-positive since Goal 2 D4).

**D8 — Divergence & anti-profile penalties (LOCKED): bounded soft-risk layer, distinct from Goal 5.** `risk_penalty` from **two independently-capped sub-penalties** (kept separate to avoid hidden double-counting): (a) **stuffer geometry** — `relu(lexical_norm − dense_evidence_norm)` + `identity_evidence_divergence` (native to Goal 4); (b) **archetype resemblance** — anti-profile cosines (identity & evidence) to {keyword-stuffer, research-only, cv_speech_robotics, consulting-only, recent-llm-only}, combined as **max + small blend bonus** (not pure max, not weighted average). Mapping: penalty-only, **`risk_modifier = clamp(1 − f(risk_penalty), 0.5, 1.0)`** — soft risk degrades fit smoothly, floor 0.5 (annihilation reserved for Goal 5/6). **Monotone, no overrides** — the JD's "unless" clauses (pre-LLM ML rescues recent-llm; product stint rescues consulting) live only in Goal 5's decisive layer; a soft false-positive is harmless because the bounded penalty is dominated by strong corroborated fit. **Non-double-count contract:** D7 = *earn positive credit*, D8 = *soft suspicion penalty*, Goal 5 = *hard decision* — deliberately different magnitudes, reinforcement = robustness not destructive compounding; D8 owns the continuous→penalty *mapping*, Goal 5 owns the hard *threshold/flag*.

**D9 — Offline eval / self-labeling harness (LOCKED): audit-driven, tripwire-guarded.** **Human top-k audits are the optimization target** — standing scorecard: top-~50 hand-categorization (strong/plausible/weak/trap), must-have coverage, trap/stuffer & honeypot incidence in top-10/100, inter-iteration movement diffs. Additions (incl. the cross-encoder feature) earn inclusion only if they improve audits **without increasing trap exposure** — **never** evaluated on estimated NDCG. A stratified **~250–400 hand-labeled tripwire set** (enriched with suspected honeypots, stuffers, narrative-concept hits, random background) has the **full competition metric computed on it but is explicitly a regression ALARM only — not an optimization target, not usable for weight-search or model selection.** Plus a **trap/adversarial unit-test suite** (canonical stuffer ranked down, Tier-5 up, each honeypot archetype excluded — pass/fail each iteration) and **3-submission discipline** (one change at a time; prefer robust over audit-maximizing weights; freeze before final; treat submissions as {safe baseline, tuned, final}).

**STATUS: Goal 4 fully detailed & locked (D1–D9, incl. D6.5).** Output artifact = the **fit bundle** per shortlisted candidate (`fit_score`, component subscores, per-criterion satisfaction + evidence trail, `engagement_modifier`, `risk_modifier` with sub-components). **Not yet built.** Next: Goal 5 (exact trap/honeypot rules + the hard thresholds D8 delegates upward).
**[Goal 7 addendum — LOCKED]:** the fit bundle additionally emits (a) per criterion **`fit_contribution`** = normalized `wᵢ·satᵢ` (positive contribution; complement `wᵢ·(1−satᵢ)` flags the weakest axis for Goal 7's concern); (b) on the evidence trail, **`evidence_id`** (from Goal 3 snippets), **`source_tier`** (the D7 Verified/Demonstrated/Declared tiers), **`surface_snippet`** ref, and **`jd_label`** (Goal 2 criterion description); (c) per candidate a semantic **`fit_tier` ∈ {Elite, Strong, Plausible, Filler}** bucketed on the **D5 tail-anchored *calibrated* fit** (thresholds stable across the 3-submission weight changes). `fit_tier` is a **relevance label, not a gating decision** — consistent with D1 (selection/eligibility stays in Goal 6). Tier thresholds resolved in the D9 audit/tuning loop.
**Carry-backs surfaced by Goal 4 (action upstream):** (1) offline/precompute must emit **pool-level distribution stats for BM25, dense-identity, dense-evidence, and cross-encoder** (for reproducible tail-anchored calibration — D5); (2) **cross-encoder scores precomputed offline over the shortlist** (D2/D3); (3) finer percentile grid deferred unless audits demand (D4).
**Goal 4 → Goal 5/6/7 carry-overs:** (1) Goal 5 owns the **hard divergence/stuffer threshold + twin-dedup threshold** and all disqualifier **overrides** (D8 stays monotone/soft). (2) Goal 6 receives `fit_score × engagement_modifier × risk_modifier` and enforces gates + honeypot-constrained top-100; engagement & risk are **separable bounded multipliers**, never fused into fit. (3) Goal 7 consumes the **per-criterion evidence trail** (sources fired + values) for extractive reasoning. (4) All audit-tuned numerics (importance ladder, θ_assess, dense-hit cutoff, soft-AND ε, attenuation floor, risk weights/floor, engagement slope) are resolved in D9 execution, not design.

### Goal 5 — Trap handling — ✅ FULLY DETAILED & LOCKED (D1–D6)
**SCOPE:** Goal 5 is the **hard, decisive RISK layer** — binary detection only, never sorts/scores. It owns: honeypot detection (deterministic logical-impossibility rules), the **one** hard-gate disqualifier that is deterministically detectable (`consulting_entire_career`) with its built-in "unless" override, and diagnostic flags. **Division of labor (locked):** Goal 4 D8 = *soft* bounded risk (monotone, floor 0.5, no overrides); **Goal 5 = *hard* risk** (binary flags + evidence, exclusion-grade); Goal 6 = *enforcement* (derives `hard_gate_flag`, applies eligibility + honeypot-constrained top-100). Goal 5 emits **primitive facts only**; Goal 6 composes the decision.

**Data-profiling findings that shaped Goal 5 (all 100k, fresh):**
- **Honeypots are cleanly captured by 3 disjoint razor-sharp rules, total 69:** `yoe − summed_tenure > 2yr` (25), `summed_tenure − yoe > 2yr` (23), `advanced/expert skill with duration_months==0, count≥2` (21). **Zero candidates trip ≥2 rules** (disjoint by construction → firing logic must be OR, not AND).
- **The distribution has a wide dead zone:** yoe−tenure p99 = 0.28yr; the buffer band **(0.5, 2.0] is empty** (0 candidates), and there are **0 candidates with exactly one** expert/adv-0-dur skill. So the threshold could sit anywhere in (0.5, 2.0) and catch the identical 69 → maximally robust, **no soft buffer needed**, ~zero false-positive risk by construction. Planted expert-0-dur always comes in batches ≥3.
- **Only 12 of the 69 are dangerous** — they wear AI/ML titles (Recommendation Systems Engineer, Applied Scientist, NLP/Search Engineer; often inflated 15–17yr YOE). The other 57 (Business Analyst, Mobile Dev…) never threaten the top-100.
- **Rejected noisy/useless checks (documented for Stage-5 defense):** role `duration_months` vs date-span mismatch fires on **99.99%** (the two fields are independently generated — unusable); skill-duration > career = 13.6%; yoe vs years-since-graduation = 10%; work-before-grad = 8.85%. Role overlaps, multi/zero-current, future dates, impossible edu years all = **0**.
- **Company-founding honeypot ("8yr at a company founded 3yr ago") is NOT detectable** — only 63 companies, no founding dates, pool-earliest-start proxy yields 0. The ~11 gap between 69 and the spec's "~80" is this undetectable type (backstopped, not chased — see D2).
- **No research/academic employers exist** (all 63 companies are product-tech / services-consulting / big-tech / fictional filler) → `pure_research_no_production` is **not deterministically detectable** → cannot be a hard gate (see D3).
- **No true twins exist:** summaries/headlines are templated (~2,761 distinct summaries across 100k; one shared by 190 people), but **identical full signatures (title+yoe+career+skills) = 0**. Single-field collisions are template reuse, not duplicate people → aggressive dedup is dangerous here (see D5).

**D1 — Honeypot detection rules (LOCKED): three deterministic checks, OR-fired.**
- **H1** `yoe − summed_tenure > 2.0`
- **H2** `summed_tenure − yoe > 2.0`
- **H3** `count(skills where proficiency ∈ {advanced, expert} AND duration_months == 0) ≥ 2`
- **Firing = logical OR** (any single check ⇒ honeypot). Required because the three populations are disjoint (AND would catch nothing) and each check is a logical impossibility, so one suffices.
- Persist **firing-rule evidence** per candidate (which rule + triggering values).
- **Explicitly documented & rejected** noisy alternatives (datespan-vs-duration, company-age, skill-vs-career, overlaps, grad-year heuristics) — the rejection list is itself Stage-5 defense.

**D2 — Honeypot enforcement + the >10% backstop (LOCKED).**
- **Goal 5 emits `honeypot_flag` + evidence; Goal 6 excludes flagged candidates from top-100 eligibility** (flooring/ineligibility, not deletion — keeps the audit trail; false-positive risk ~nil per the empty dead zone).
- **No soft buffer band** (dead zone is empty). **No hardcoded ID blocklist** (spec says "don't special-case them"; our rules are *general logic*, defensible).
- **The ~11 undetectable (company-age) honeypots are backstopped *architecturally*, with no dependence on manual review:** (1) **Goal 4 D7 corroboration-first scoring is the primary defense** — a fabricated profile must still earn Tier-1 (assessment) or Tier-2 (demonstrated career-evidence) to satisfy must-haves; fake *tenure* earns neither, so it sinks. (2) **Goal 6 targets ≪10% with headroom** — all 69 detected are excluded (detected-rate in top-100 = 0), so even 1–3 undetectable slips stay far under the line.

**D3 — Hard disqualifier gates + "unless" overrides (LOCKED): exactly one non-honeypot hard gate.**
- **`consulting_entire_career` = HARD gate with built-in override.** Deterministic via Goal 3 `company_table` (`is_consulting_entire_career` ⇒ *every* stint is services-consulting; any product/big-tech/fictional stint breaks it → only the ~9.7% all-consulting fire). Override = `any_product_stint` (the JD's "unless you have prior product-company experience"). JD-faithful ("entire career… we're not a fit").
- **`pure_research_no_production` DEMOTED to soft.** JD wants it absolute, but with **no research/academic employer category in the data it cannot be detected deterministically**; a hard gate on a fuzzy archetype-cosine would false-exclude real production-ML PhDs (~17.5k have PhDs). Handled as a Goal 4 soft penalty (research-only anti-vector) and self-corrects (no Tier-2 production corroboration → sinks). Limitation documented for Stage 5.
- **The other five strong-negatives stay soft (Goal 4 D8), no explicit hard override needed:** `recent_llm_only`, `senior_not_coding_18mo`, `title_chaser`, `cv_speech_robotics_no_nlp`, `closedsource_5yr` — all phrased "probably not" in the JD. Their "unless" clauses **self-resolve**: the penalty is bounded (≥0.5), so a candidate who also has the rescuing evidence earns positive corroboration that mathematically dominates.
- Goal 5 emits **override-suppression evidence** even when a gate is suppressed (e.g. `consulting_gate_suppressed: product stint at Razorpay`) for Goal 7 reasoning + defense.

**D4 — Stuffer: no hard gate (LOCKED).** Goal 4 delegated a "hard divergence/stuffer threshold" to Goal 5; **resolved as: no hard stuffer gate.** Stuffers are over-claims, not logical impossibilities, and are already neutralized twice softly — Goal 4 D7 attenuates Tier-3-only evidence to a floor (must-have soft-AND collapses → near-zero fit), and Goal 4 D8 applies the divergence/archetype penalty. A hard cutoff on a continuous, noisy divergence would false-exclude rich-vocabulary genuine fits and is harder to defend than the layered soft story. Goal 5 instead emits a **diagnostic-only `stuffer_suspect_flag`** (high lexical−dense divergence AND zero Tier-1/2 corroboration AND title/domain mismatch) — used **only** for audit + Goal 7 reasoning, never exclusion; threshold low-stakes, set during the D9 build (needs runtime embeddings).

**D5 — Twins: removed from Goal 5 (LOCKED).** Aggressive dedup is dangerous on this templated data (would collapse legitimately-distinct candidates), there are **zero true full-profile duplicates** to remove, and two near-identical *relevant* candidates both surfacing doesn't hurt NDCG/MAP. Twins are a *selection redundancy*, not a *risk* → **reassigned to Goal 6** as an **optional, strict, near-no-op safeguard** (collapse only candidates identical across career sequence AND skill set AND signal envelope simultaneously; keep best-engagement representative; expected to essentially never fire). Goal 5 stays scoped to honeypots + the consulting gate.

**D6 — Output contract + validation (LOCKED): store primitives, derive decisions, validate by invariants.**
- **Stored primitive facts only** (offline-precomputed over all 100k from Goal 3 structured features; aligned by candidate_id / row order): `honeypot_flag`, `honeypot_reasons[]` (rule id + triggering values), `consulting_gate_flag`, `consulting_gate_suppressed`, `suppression_evidence[]`. The diagnostic `stuffer_suspect_flag` is emitted at runtime alongside Goal 4's bundle (needs lexical−dense divergence).
- **Goal 6 derives** `hard_gate_flag = honeypot_flag OR (consulting_gate_flag AND NOT consulting_gate_suppressed)` — not stored, to avoid persisting a composed decision.
- Manifest discipline mirrors Goals 1–3 (sha256 of artifact + upstream Goal 3 hashes, rule-version, git commit, timestamp).
- **Validation (no labels, no runtime manual dependency):** (1) per-rule **unit tests** on crafted fixtures; (2) **invariant asserts** — `H1 fires ⇒ yoe−tenure > 2.0`, `H2 ⇒ tenure−yoe > 2.0`, `H3 ⇒ expert_zero_duration_count ≥ 2`, **no candidate in the (0.5, 2.0) gap**; (3) **distribution-separation** checks. The observed **count of 69 is an audit statistic, not a harness gate** — invariants expose a tenure-computation regression more directly than a count mismatch. Plus the adversarial unit-test suite + dev-time ablation from Goal 4 D9.

**STATUS: Goal 5 fully detailed & locked (D1–D6).** Output: offline `risk_flags` artifact (honeypot + consulting primitives + evidence) aligned by candidate_id; runtime diagnostic `stuffer_suspect_flag` in the Goal 4 bundle. **Not yet built.**
**[Goal 8 addendum — LOCKED, verified non-breaking]:** the per-candidate risk rules (H1/H2/H3 honeypot, consulting gate + override — D1/D3) are factored as a reusable `risk_flags(candidate_features)` function (pool-independent — every rule is a per-candidate logical test over Goal 3 features), so the **Sandbox Runtime** (Goal 8 D8) computes identical flags for a ≤100 sample. The observed pool counts (69 honeypots, etc.) remain an **offline audit statistic** (D6), not part of the per-candidate function. No conflict with D1–D6.
**Goal 5 → Goal 6/7 carry-overs:** (1) Goal 6 derives `hard_gate_flag` from the stored primitives and owns eligibility, the honeypot-constrained top-100, and the optional twin safeguard reassigned from Goal 5. (2) Goal 7 consumes suppression evidence + diagnostic flags for the survivors (all `hard_gate_flag=false`). (3) `pure_research` + the five strong-negatives remain Goal 4 soft penalties — Goal 5 adds no hard layer for them.

### Goal 6 — Ranking & selection — ✅ FULLY DETAILED & LOCKED (D1–D7)
**SCOPE:** the **decision policy** — the only goal that converts scores into the submission. Consumes Goal 4 fit bundle (`fit_score`, `engagement_modifier`, `risk_modifier`, per-criterion evidence trail) + Goal 5 risk primitives (`honeypot_flag`, `honeypot_reasons`, `consulting_gate_flag`, `consulting_gate_suppressed`, `suppression_evidence`). **Canonical flow: score everyone → mark eligibility → filter eligible → sort → take top 100 → (Goal 7 reasoning) → assemble final dataframe → validate → write CSV.** `final_score = fit_score × engagement_modifier × risk_modifier` is computed for **all** candidates; **selection is restricted to the eligible subset**.

**Data-profiling findings that shaped Goal 6 (all 100k, fresh re-run):**
- **Eligible after hard gates = 90,197** (9,803 ineligible) → ~900× headroom over the 100 needed; **filling the top-100 is never at risk.**
- **All 69 detected honeypots are excluded** (H1=25, H2=23, H3=21, disjoint OR) → detected honeypot rate in top-100 = **0** (≪10% satisfied automatically with headroom). **12 of the 69 carry relevant ML/AI titles** (the dangerous ones) — all excluded. The ~11 undetectable company-age honeypots are backstopped by Goal 4 corroboration-first scoring (no Tier-1/2 evidence → they sink), not by selection logic.
- **Consulting-entire-career = 9,745** before the product-stint override.
- **Zero true full-profile twins** (career-sequence + skillset + signal-envelope identical = 0 across 100k) → diversity/dedup is a genuine no-op.

**D1 — Eligibility & hard-gate enforcement (LOCKED): floor, never delete; derive eligibility only in Goal 6.**
- **Never delete** candidates from the master frame; **preserve all Goal 5 primitive detection outputs verbatim**.
- **Eligibility derived only inside Goal 6:** `hard_gate_flag = honeypot_flag OR (consulting_gate_flag AND NOT consulting_gate_suppressed)` — **not persisted** (a composed decision stays ephemeral, per Goal 5 D6). Select from the eligible subset.
- **`selection_manifest.json`** emits: total candidates, eligible count, excluded count, **exclusions by reason**, and **top excluded candidate scores** (audit artifact defending gate decisions).
- **Asserts:** exactly **100 rows**; **zero `honeypot_flag==true`**; **zero `hard_gate==true`** in the selected set.

**D2 — Output `score` column (LOCKED): emit `final_score`, no rescaling, with a deterministic tie-encoding micro-term.**
- Metrics use rank order vs hidden ground truth; the numeric score only needs to be **non-increasing with rank** (spec §3). Emit **`final_score`** — **no min-max scaling, no Goal-6 recalibration**. Calibration lives entirely in Goal 4 D5 (tail-anchored); Goal 6 presents only. Monotonicity guaranteed by sorting on the emitted key.
- **Tie-encoding (resolves the validator conflict — see D4):** corroboration breadth is folded into the printed score as a **deterministic, strictly-ordered sub-epsilon micro-term** (e.g. `printed_score = final_score − rank·δ`, δ ≪ smallest meaningful score gap), so the full deterministic sort is encoded *in the score itself* and printed scores are effectively unique. This keeps breadth influential **without** a post-score reordering that could violate `candidate_id` ordering. The micro-term preserves non-increasing-with-rank and is audit-documented; any residual exact-tie case (identical `final_score` **and** breadth) is broken by `candidate_id` ascending only.

**D3 — Honeypot ≪10% (LOCKED): automatic via hard exclusion, no quota juggling.**
- All detected honeypots excluded at D1 → detected rate = 0; **no constraint/quota selection logic**. Undetectable company-age honeypots backstopped architecturally (Goal 4 D7). The D1 assertion enforces zero honeypot in top-100. Diagnostic `stuffer_suspect_flag` survivors logged for audit only.

**D4 — Tie-breaking (LOCKED, REVISED): candidate_id ascending on equal printed score — validator-conformant.**
- **Why revised:** the provided `validate_submission.py` (lines 136–144) **rejects** any two rows with equal `score` that are not in **`candidate_id`-ascending** order. This is **stricter than the prose spec §3** ("a secondary signal from your model, *or* candidate_id ascending") and, being the Stage-1 auto-validator, is **ground truth**. A corroboration-breadth-first tie-break (prior D4) could place a higher `candidate_id` before a lower one at an equal printed score → auto-reject. Exact ties are most likely in the **filler tail** (ranks ~60–100), where modifiers collapse to neutral.
- **Resolution:** the deterministic **sort key** is `final_score` desc → corroboration breadth desc → `candidate_id` asc, but **breadth is expressed through the printed-score micro-term (D2)**, not as a post-score reordering. Therefore the **only** tie-break acting on an exactly-equal *printed* `score` is **`candidate_id` ascending** — exactly what the validator enforces. **`fit_score` is still never a tie-break.**
- Net effect: breadth still influences order (via the score), and the emitted CSV always satisfies `validate_submission.py`.

**D5 — Diversity / twins (LOCKED): no diversity constraint, no dedup.**
- Profiling shows **zero true full-profile duplicates** → no deduping required and none applied. NDCG/MAP don't reward diversity; collapsing templated near-duplicates would be harmful on this data. No twin safeguard shipped; the zero-twins finding is documented for Stage-5 defense.

**D6 — Filling exactly 100 (LOCKED): always fill, with honest filler.**
- Spec requires **exactly 100**; truly-relevant candidates are far fewer. Fill ranks past the relevant tier with the **best-available eligibles by `final_score`**. Goal 7 reasoning marks fillers honestly (≥1 limitation acknowledged) — matches the spec's rank-100 example and the Stage-4 rank-consistency check.

**D7 — Output contract & validation (LOCKED): validate the EXACT final dataframe, by invariants, with CSV round-trip.**
Pipeline: select → generate reasoning (Goal 7) → **assemble final dataframe → validate → write CSV**. Run the provided `validate_submission.py` **PLUS**:
1. **Final-artifact validation** — validate the exact dataframe that will be written (not an intermediate).
2. **Finite-score check** — all scores finite (no NaN/inf).
3. **Rank-order consistency** — recompute ordering with the production sort logic; assert emitted ranks match exactly.
4. **Eligibility consistency** — for every selected candidate assert `not honeypot_flag` and `not hard_gate_flag`, validated **directly against Goal 5 primitive flags** (not merely "absent from an exclusion list").
5. **Top-100 correctness** — `lowest_selected_score ≥ highest_unselected_score` (with tie-break consideration).
6. **Candidate alignment** — every selected id exists in `candidate_ids.npy`, Goal 1 parquet, and the Goal 3 feature matrix.
7. **Duplicate-score tie-break audit** — for any exactly-equal printed `score`, verify ordering is **`candidate_id` ascending** (per revised D4 — matches `validate_submission.py` lines 136–144; corroboration breadth is already encoded in the score via the D2 micro-term, `fit_score` intentionally excluded).
8. **Reasoning checks** — non-empty; uniqueness; **reasoning-rank consistency** (top candidates contain positive evidence; bottom fillers contain ≥1 limitation — catches reasoning inversion).
9. **CSV round-trip** — write a temp CSV, reload, re-run all spec checks, then emit the final file.
10. **Highest-scoring excluded candidate** recorded (id, score, exclusion reason) for gate-decision defense.
11. **Selection provenance** — every selected candidate must have a **fit bundle + evidence trail + reasoning**; none enters the top-100 without explainability artifacts.
Manifest discipline mirrors Goals 1–5: `selection_manifest.json` (config hash, counts, exclusions-by-reason, top-excluded, git commit, timestamp). **Config-agnostic across the 3-submission discipline** (Goal 4 D9) — Goal 6 logic is frozen; only Goal 4 weights vary between {safe, tuned, final}.

**STATUS: Goal 6 fully detailed & locked (D1–D7).** Output artifacts: `submission.csv` (`candidate_id, rank, score, reasoning`), `selection_manifest.json`. **Not yet built.**
**[Goal 7 addendum]:** Goal 6 **consumes `fit_tier` from Goal 4** (does not recompute it) and passes it into the Goal 7 contract; **`filler_flag ≡ fit_tier==Filler`** — the D6 "relevant-tier cutoff" is exactly the Filler boundary, so no separate filler computation is introduced.
**Goal 6 → carry-overs:** (1) Goal 7 reasoning is consumed at assembly time (validation #8, #11). (2) Goal 4 must surface a **corroboration-breadth count** in the fit bundle — Goal 6 folds it into the printed-score micro-term (D2), not a post-score tie-break (revised D4). (3) Goal 6 logic is frozen across all 3 submissions.

### Goal 7 — Reasoning generation — ✅ FULLY DETAILED & LOCKED (D1–D8)
**SCOPE:** turn the top-100 selection into the CSV `reasoning` column — a 1–2 sentence justification per candidate. Goal 7 is a **deterministic evidence-to-narrative compiler**: it consumes an already-decided evidence contract from Goals 4–6 and *projects* it into prose. It **never reads raw candidate data and never selects evidence** — so reasoning can never drift from the ranking logic. Runs **at runtime inside the budget** (5 min / 16 GB / CPU / no network), but cost is trivial (100 rows). Judged at **Stage 4** on 10 random rows against 6 checks: specific facts, JD-connection, honest concerns, no hallucination, variation, rank-tone consistency.

**Data-profiling findings that shaped Goal 7 (all 100k):**
- **title / YOE / current_company / summary present for 100%** → every row always has a citeable identity anchor and real narrative facts (summary ~463 char median) to extract.
- **`skill_assessment_scores` only 24.2%** → Verified-tier corroboration is high-trust *when present* but never assumable; **`skills[]` diluted ~12k×/skill → NEVER citeable** (naming a skill from it = the exact "hallucination" the rubric penalizes).
- **notice_period present 100%, >60d for 61.6%** → an honest, citeable concern is essentially always available; honest-concern coverage is never the bottleneck.
- **Concept rarity** (ranking/search ~1%, embeddings ~0.2%, eval ~0.07%) → feeds the *distinctiveness* term so rare corroborated hits are foregrounded in the explanation.

**D1 — Generation method (LOCKED): deterministic evidence-to-narrative compiler, not a template engine.** Input = a structured evidence contract (IR). Intermediate representation = facts tagged with **roles** (`primary_strength`, `secondary_strength`, `jd_link`, `concern`, `context_modifier`). Surface realization = a rank-conditioned **phrase grammar**. *Rejected:* extractive draft + LLM polish (no legitimate offline phase — top-100 is only known post-ranking; hosted LLM violates no-network; hand-polish breaks single-command reproduction); in-budget local LLM (reintroduces hallucination, nondeterminism, Stage-3 fragility for cosmetic gain on 100 rows). **Invariant:** reasoning is a pure projection of already-decided evidence.

**D2 — Evidence contract + two-stage split (LOCKED).** Goal 7 = **7A Explanation Planner** (deterministic; role-tags & orders the contract into the IR) + **7B Surface Realizer** (phrase grammar → string). Contract per selected candidate: decision/identity block (`candidate_id`, `rank`, `final_score`, `fit_score`, **`fit_tier`** [emitted by Goal 4], `filler_flag` ≡ `fit_tier==Filler`; `rank_band` retained audit-only); always-on anchor literals (`current_title`, `years_of_experience`, `current_company`+`company_type`, `product_tenure_yrs`); per-criterion **strength evidence** `{criterion_id, jd_label, class, importance, satisfaction_degree, fit_contribution, evidence:[{evidence_id, source_tier (verified/demonstrated/declared), source_type, field_path, raw_value, surface_snippet}]}` (the `jd_label` makes JD-connection automatic); modifiers (`engagement_modifier`+top signal, `risk_modifier`+top component, `consulting_gate_suppressed`+`suppression_evidence`); audit-only `stuffer_suspect_flag`. **Every evidence atom carries a stable `evidence_id`** (minted offline in Goal 3, stable across the pipeline); the planner binds each rendered clause to its `evidence_id` (provenance side-artifact, not in CSV) → mechanically auditable grounding. `surface_snippet` is **precomputed offline in Goal 3** for criterion-linked narrative spans, and is null when none exists → the realizer falls back to structured facts (runtime never reopens raw text). Stuffer/honeypot diagnostics are **audit-only, never rendered**; `suppression_evidence` IS rendered as positive context. *Rejected (Option B):* Goal 6 emits the role-tagged IR — pushes presentation into frozen Goal 6 and splits the compiler across goals.

**D3 — Explanation Planner internals (LOCKED).** **Explanation-strength** (ordering of what to *mention*, distinct from scoring weight) = **`fit_contribution × trust_tier_factor × distinctiveness × citeability`**. **`fit_contribution` = the criterion's *positive* contribution to `fit_score`, defined as normalized `wᵢ·satᵢ`** (its weighted satisfaction as a share of the total) — **NOT** log-share/marginal-drag, which in a soft-AND foregrounds the *weakest* axis (the opposite of a showcase strength). Rank-drivers float to `primary_strengths` by construction → **no must-have guard needed** (rejected). The complement `wᵢ·(1−satᵢ)` identifies the weakest axis and feeds **concern-ladder rung 4** — one quantity serves both strengths and concern. `trust_tier_factor` rewards Verified/Demonstrated over Declared; `distinctiveness` uses frozen pool concept-frequency stats; `citeability` rewards a concrete snippet over vector-only. **Concern-priority ladder (deterministic, first applicable):** (1) `filler_flag` → below-cutoff acknowledgement; (2) material surviving soft-risk; (3) context-modifier miss (notice>60d / location / YOE band-edge / stale engagement); (4) **weakest-evidence deficit** (framed as an *evidence deficit in recruiter language*, e.g. "limited demonstrated evidence of production retrieval at scale" — never a criterion name, never "missing", always "lighter"); (5) none material. One primary concern; tone-calibrated by band (D5).

**D4 — Citeable-field whitelist (LOCKED).** Realizer may cite ONLY: anchor literals; `skill_assessment_scores` (named skills allowed *only* here, Verified tier); career narrative snippets (titles/descriptions, Demonstrated tier); `redrob_signals` values. **Raw `skills[]` array is never citeable.** Every cited token must resolve to an `evidence_id`.

**D5 — Rank-band tone calibration (LOCKED): four bands, upstream-driven.** Goal 7 consumes a **semantic `fit_tier` ∈ {Elite, Strong, Plausible, Filler}** emitted upstream (scorer owns the boundaries; Goal 7 computes nothing from raw scores → single source of truth). Tone tracks **fit quality, not seat position.** Concern rendering conditioned on band: **Elite** → concern optional, framed as a *consideration*; **Strong** → framed as a *caveat*; **Plausible** → concern *required*; **Filler** → *limitation + below-cutoff acknowledgement required*. *Rejected:* fixed rank cutoffs (ties tone to position → over-praise risk); raw-score thresholds inside Goal 7 (breaks single-source-of-truth).

**D6 — Surface Realizer (LOCKED): slot-grammar, evidence-driven first.** Sentence skeleton (≤~240 char): **S1** = adaptive anchor + 1–2 primary strengths with JD link; **S2** = secondary/engagement + band-conditioned concern. **Evidence-driven phrase selection first**, `candidate_id`-hash **tiebreak only** for residual phrasing variety. **Separate realization paths per trust tier** (Verified "scored 88 on the IR assessment" / Demonstrated "shipped vector search at Razorpay" / Declared hedged "describes…/lists…"). **Adaptive anchor** — not always title+YOE+company; lead with whatever is most informative (sometimes the strength leads). Variation primarily **content-driven**, banks secondary; all rendered facts stay bound to `evidence_id` provenance. **Snippets render only when precomputed (Goal 3); otherwise the realizer falls back to structured facts** (title/YOE/company/assessment/signals) — graceful degradation, never a gap. *Rejected:* single fixed frame (the penalized template); lightweight CFG (awkward-output risk, harder to defend).

**D7 — Validation & anti-hallucination harness (LOCKED): two tiers + audit report.** **Hard invariants (fail the build):** grounding/evidence resolution (every cited token resolves to an `evidence_id`; named skills ∈ assessment set; numbers equal source fields; zero `skills[]` tokens), numeric correctness, provenance completeness, uniqueness, CSV validity. **Soft diagnostics (warn only):** diversity, skeleton concentration, tone distribution, phrase-bank usage. Cheap invariants run **in-runtime as hard asserts** (non-empty, unique, length, every clause has `evidence_id`, finite — loud fail like other goals); richer checks run in the offline build/audit harness. Emits a **`reasoning_audit_report.json`** flagging suspicious rows for human review (no LLM judge — human-in-the-loop, consistent with Goal 4 D9). Satisfies Goal 6 validation #8 (reasoning-rank consistency) and #11 (selection provenance).

**D8 — Output mechanics & reproducibility (LOCKED).** Reasoning folds into Goal 6's `submission.csv`; Goal 7 never touches `score` (Goal 6's raw `final_score`). **Length: adaptive 1–2 sentences, ~240-char cap** — S1 always; S2 only on a real secondary strength or a required concern (band rules drive structure; no padding on thin profiles). *Rejected:* always-two-sentences (forces empty S2 = soft-templating); tight single sentence (too dense). Write via `csv.writer` **`QUOTE_ALL`**, UTF-8, snippet newlines collapsed to spaces. Fully **deterministic** (evidence-driven selection + pinned `candidate_id`-hash tiebreak; version-controlled phrase banks + grammar; no RNG). Side artifacts: `reasoning_provenance.json` (clause→`evidence_id`), `reasoning_audit_report.json`; manifest discipline (sha256, grammar/bank version, git commit, timestamp) mirrors Goals 1–6.

**STATUS: Goal 7 fully detailed & locked (D1–D8).** Output: the `reasoning` column in `submission.csv` + `reasoning_provenance.json` + `reasoning_audit_report.json`. **Not yet built.**
**Goal 7 → upstream carry-backs (RESOLVED — see addenda on Goals 3/4/6):** (1) **Goal 4** emits per-criterion **`fit_contribution` = normalized `wᵢ·satᵢ`** (positive contribution, not log-share) — drives explanation-strength ordering; complement feeds the concern. (2) **Goal 4 (the scorer)** emits the semantic **`fit_tier` ∈ {Elite, Strong, Plausible, Filler}**, thresholded in its tail-anchored *calibrated* fit space (stable across the 3-submission weight changes); `fit_tier` is a **relevance label, not a gating decision** (Goal 6 still owns selection), and `filler_flag ≡ fit_tier==Filler`. (3) **Goal 3** precomputes **criterion-linked `surface_snippet`s with stable `evidence_id`s** (sidecar artifact; runtime never reopens raw text); Goal 4's trail threads `source_tier` (already its D7 tiers) + `jd_label` (Goal 2 criterion description). **Accepted consequence (Stage-4 watch-list):** tone (`fit_tier`/fit) and rank (`final_score`) can mildly diverge via the bounded engagement/risk multipliers — intended per D5; bounded by the [0.7,1.1]/[0.5,1.0] caps.

### Goal 8 — Compute optimization — ✅ FULLY DETAILED & LOCKED (D1–D9)
**SCOPE:** the runtime/compute-engineering contract spanning the whole pipeline — defines the offline↔runtime boundary, the two execution environments, retrieval/scoring compute, determinism, the budget envelope, and end-to-end reproducibility. Owns *how* the system runs within ≤5 min / ≤16 GB / CPU / no-network (§3) while pushing all heavy work into the unbudgeted offline phase (§10.3). Does **not** own scoring logic/weights (Goals 4–6) — only their execution envelope.

**Performance-profiling findings that shaped Goal 8 (measured at true data shapes on a 2-core/3.8 GB sandbox — deliberately weaker than the 16 GB target, so all numbers are conservative):**
- **Goal 8 is not compute-bound.** Total in-budget work ≈ **1–2 s cold wall-clock vs the 300 s limit (~150–300× headroom)**; peak memory ≈ **~1.3 GB vs 16 GB (~10×)**; disk ≈ **0.5–1 GB vs 5 GB**.
- Embeddings 100k×384 f32 = **153.6 MB each** (identity+evidence = 307 MB); full `np.load` ~33 ms, mmap ~0.1 ms. Feature matrix 100k×90 = 36 MB, ~9 ms. BM25 CSC ~20M nnz ≈ 160 MB.
- Exact brute-force cosine (ideal + 5 archetypes, both embeddings) over full 100k = **~74 ms**; BM25 fixed ~40-term query via term-major CSC gather = **~0.2 ms**; top-k via `argpartition` = **~0.4 ms**; weighted aggregate 100k×90 = **~1.3 ms**.
- The single largest line item is the quantile transform: **~433 ms full-pool vs ~30 ms on the ~5–8k shortlist** → re-ranking belongs on the shortlist (D4), but even full-pool fits.
- **The shortlist is no longer a budget necessity** — even exhaustive full-pool scoring is sub-second; the cascade survives for architecture/defensibility/future-proofing, not compute (D4).

**D1 — Phase boundary contract (LOCKED): two phases; runtime is pure load-and-arithmetic.** The in-budget ranking step performs **no candidate-dependent embedding or model inference at runtime** (the ban is on *candidate-dependent* inference, not embedding in general — leaving the door open for the Sandbox path and the already-frozen JD vectors). It loads frozen artifacts → vectorized array arithmetic → union shortlist → Stage-B re-rank → Goal 5/6 gates → top-100 → Goal 7 reasoning → CSV (~1–2 s).
- **Runtime ML Policy (LOCKED, behavioral — not a package allowlist):** the ranking step is a **frozen scorer, not an ML pipeline.** It must **not instantiate, load, download, or execute any neural model or inference engine** — transformers, sentence-transformers, Torch, ONNX, GGUF, or any other — to compute candidate-dependent outputs at runtime. **ALL** neural computation (embedding generation, reranking, calibration fitting, model inference) happens **offline**; at runtime the system only loads precomputed artifacts (embeddings, indexes, feature matrices, lookup tables, calibration params) and performs deterministic retrieval/filter/score/rank/reason/CSV.
- **Dependencies are constrained by behavior, not by a fixed package list.** The only hard runtime limits are CPU-only / no-network / ≤5 min / ≤16 GB / ≤5 GB disk; **any CPU-only, offline, deterministic library is permitted** (numpy, scipy, pyarrow, pandas, joblib, …). A **lean, model-free dependency set is preferred** (smaller, more deterministic Stage-3 image) but **not mandated** — *the prior "numpy/scipy only" wording is superseded by this behavioral policy.* Decision rule (mirrors cross-cutting principle #2): **when in doubt push computation into precompute, not runtime; between two rule-compliant implementations prefer the simpler.**

**D2 — CE compute contract (LOCKED).** If enabled, CE is computed **offline over the entire released pool**; Competition Runtime performs **lookup only**; Sandbox Runtime computes CE on uploaded samples (only when enabled). **Shortlist-only precompute rejected** (no offline saving — offline is unbudgeted — and fragile coupling to the retrieval config across the 3-submission tuning). Inclusion is a **Goal 4 D9 audit decision; default OFF** until it shows measurable ranking value without increased trap exposure. **Artifact = `ce_features.npy [N, C]`** (2-D, row-aligned; C may be 1 initially; reserved cols: CE relevance score, percentile, calibrated score, margin-over-null) — extensible without an artifact-format break; active cols documented in the feature manifest.

**D3 — Retrieval = exact brute-force (LOCKED).** All full-pool similarity operations are **exact and vectorized unless profiling demonstrates a runtime violation** — exact numpy over L2-normalized embeddings (dot=cosine), top-k via `argpartition` (membership only), BM25 via term-major CSC gather, structural prefilter as a boolean mask. **ANN (FAISS/HNSW) rejected by default** — no speed need at N=100k; adds recall risk (could silently drop a ~1%-rare Tier-5 from the recall-first shortlist), nondeterminism, and a dependency that violates D1. Rejection rationale documented for Stage-5.

**D4 — Three-tier allocation; explicit cascade retained (LOCKED).** **Tier 1** (offline → runtime lookup, zero runtime compute): embeddings, BM25 index, feature matrix, frozen pool-stats, anti-profile vectors, company_table, jd_query, Goal 5 risk flags, CE features (if on). **Tier 2** (full-pool at runtime, cheap): similarity/retrieval channels to form the shortlist (~75 ms) + a cheap full-pool relevance proxy for the audit manifest. **Tier 3**: Stage-B re-ranking (criterion satisfaction, fusion, modifiers, `final_score`) on the **recall-first union shortlist only** (Goal 4 D3). The cascade is kept for **JD-v2 mirroring, future-heavy-reranker bounding, and stage ablatability — explicitly not for budget** (profiling shows full-pool also fits; that is the documented fallback). Goal 6 D2 reconciled: full `final_score` on shortlist; full-pool proxy retained for excluded-score auditing. **Fallback principle (LOCKED):** even if future revisions remove *all* shortlist-only operations (CE permanently off + fully-vectorized Stage-B), the architecture **may retain the shortlist boundary for separation-of-concerns** — absence of a computational reason is not, by itself, grounds to delete the retrieve/re-rank seam.

**D5 — Loading & memory (LOCKED).** **Full-load** densely-scanned matrices (embeddings ×2, features — we matmul every row); **mmap** sparse/selective artifacts (BM25 CSC → ~40 cols touched; the **row-addressable snippet sidecar** → ~100 rows materialized, the only meaningful memory win). Competition Runtime **never loads raw `candidates.parquet`/JSONL** (snippets + features replace it — the largest potential memory item is excluded by construction). Dtypes **f32** (embeddings/features), **int32** (ids); f16 a documented lever only if memory ever tightens. Peak ~1.0–1.5 GB vs 16 GB. **Two-phase load order (LOCKED):** (1) startup validation load — read the *small* artifacts first (manifests, frozen stats, jd_query, candidate_ids), run **hash + alignment + shape checks**; (2) only on success, load large matrices and execute. Flow `load manifests → validate → load matrices → execute` — a broken/misaligned/stale artifact fails loudly in ~100 ms instead of after hundreds of MB (consistent with Goals 1 & 3 loud-failure philosophy).

**D6 — Determinism (LOCKED).** No randomness, clocks, locale dependence, or runtime-derived statistics. Stable hashing only (`zlib.crc32`/`hashlib`, never salted `hash()`); `PYTHONHASHSEED=0`. Deterministic collection ordering (`np.unique` for unions, never set-iteration order). Frozen artifacts + frozen pool statistics. Pinned Python/NumPy/SciPy/Docker environment. Deterministic tie-break hierarchy (Goal 6 D4). **Single-thread BLAS preferred and enabled in the canonical reproduction image — a reproducibility-hardening choice, NOT the primary correctness guarantee.** Primary guarantee of identical rankings = **fixed artifacts + deterministic logic + deterministic tie-breaking**; single-thread BLAS only removes residual float-reduction jitter. **Reproduction validation:** (1) canonical Docker run → exact output-hash match; (2) semantic validation → identical ranking / top-100 membership. Binds Competition Runtime; Sandbox Runtime exempt.

**D7 — Budget accounting + guards (LOCKED).** Budget documented from measured profiling (~1–2 s cold, ~1.3 GB peak); **cold-start path is the canonical budget** (model-free import, no neural-inference engine loaded — per the D1 Runtime ML Policy). Runtime **always attempts to produce the CSV**; timing and memory ceilings are enforced in **CI/regression testing, not by self-aborting the production run** (a valid CSV <300 s still scores; a hard memory check ~8 GB stays, since exceeding 16 GB = DQ). Startup invariant checks validate artifact integrity + alignment (D5). Emit **`runtime_report.json`** every run (per-stage timings, peak RSS, bytes loaded, thread/cold flags). Budget numbers documented as **conservative** (weaker-than-target hardware). **`runtime_report.json` + manifests = the Stage-3 / Stage-5 evidence package.**

**D8 — Two runtimes, shared ranking logic, separate dependency sets (LOCKED).** **Competition Runtime** = D1 artifact-only ranking step (the submission + Stage-3 reproduction); constraints = the 5-min/16 GB/CPU/no-network contract + the D1 Runtime ML Policy (no runtime neural inference); deps = a **lean, model-free, CPU-only/offline set** (numpy/scipy/pyarrow/pandas/joblib as needed) — constrained by behavior, not a fixed allowlist. **Sandbox Runtime** = end-to-end demonstration on a ≤100 arbitrary sample (§10.5); constraints = ≤5 min CPU, no hosted-LLM calls; deps = may carry model(s) + heavier deps. They **share ranking logic (Goals 4→7 byte-identical)** but **not dependency sets**. Sandbox runs the Goal 1→3→5 builders on the sample on the fly (candidate-dependent embedding permitted here per D1), then applies **shipped frozen pool-stats** (normalization, tail-anchor calibration, concept freq, anti-profiles, company_table, jd_query, weights) — **never recomputes them from the sample** (this actively *proves* the transforms are pool-frozen). Optional pool-subset lookup fast-path. **Hosting platform NOT locked** — must satisfy §10.5 (hosted env *or* a `docker run` recipe in README; the Docker image doubles as the Stage-3 repro environment). Profiling: sandbox embed ≈ ~100×6 docs ≈ 3–12 s + optional CE ~100 pairs — well inside 5 min.

**D9 — Manifest & reproducibility (LOCKED).** Two-command repro (`precompute.py --candidates …` unbudgeted → `rank.py --artifacts … --out submission.csv` budgeted), both documented + shipped (§10.3-compliant). **`build_manifest.json` is the SOLE top-level entry point** — the one file an organizer opens first; everything hangs from it. It references every sub-manifest + report by path and carries the hash chain + provenance, e.g. `{ "candidates_sha256", "goal1_manifest", "goal2_manifest", "goal3_manifest", "goal5_manifest", "runtime_report", "ce_features_sha256?", "submission_sha256", "git_commit", "lib_versions", "model_revision", "thread_settings", "timestamps" }` — no tooling needs to discover sub-manifests independently. Startup **artifact↔pool binding assertion** (loaded artifacts' source-hash matches the candidates file + `candidate_ids` align across all artifacts; loud fail before any matrix loads — satisfies §10.3 "cover exactly the released pool"). **Reproduction tiers:** ranking-step from shipped artifacts = **bit-identical** (canonical Docker) / **semantic** (else, D6); full-from-raw (`precompute`→`rank`) = **semantic-equivalent** (embedding float nondeterminism per Goal 3 D9 documented; shipped artifacts are canonical). **Two pinned dependency sets** (Competition Runtime = lean model-free CPU-only/offline set per the D1 Runtime ML Policy — its Dockerfile = the Stage-3 image; precompute + Sandbox carry torch/transformers/sentence-transformers + CE model if enabled). Evidence package = sub-manifests + `build_manifest.json` + `runtime_report.json` + determinism self-test result.

**STATUS: Goal 8 fully detailed & locked (D1–D9).** Output artifacts/entrypoints: `precompute.py`, `rank.py`, Competition Runtime Dockerfile (= Stage-3 image), two pinned `requirements` files, `build_manifest.json` (sole root), `runtime_report.json`, the Sandbox app + (deferred) hosting choice. **Not yet built.**
**Goal 8 → upstream carry-backs (verified deliverable, folded into Goals 1/3/5):** (1) **Goal 3** — `evidence_snippets` sidecar must be **row-addressable** (O(100) random access, D5); per-candidate **feature/embedding/snippet derivation factored as reusable `builder(candidate, frozen_inputs)` functions** (D8). (2) **Goals 1/5** — same builder-refactor for Sandbox parity. None conflict with locked decisions (verification notes recorded in each goal's Goal-8 addendum).

---

## Status summary
**Fully detailed & locked:** **Goal 1** (data ingestion, D1–D5), **Goal 2** (JD understanding, D1–D8), **Goal 3** (candidate representation, D1–D9), **Goal 4** (candidate↔JD scoring, D1–D9 incl. D6.5), **Goal 5** (trap handling, D1–D6), **Goal 6** (ranking & selection, D1–D7), **Goal 7** (reasoning generation, D1–D8), and **Goal 8** (compute optimization, D1–D9). **All 8 goals now fully detailed & locked.**
**Goal 8 → upstream carry-backs (RESOLVED, verified deliverable, folded into Goals 1/3/5):** two named runtimes — **Competition Runtime** (artifact-only, numpy/scipy, the 5-min reproduction contract) and **Sandbox Runtime** (end-to-end on a ≤100 sample, may carry models) — share ranking logic (Goals 4→7) but not deps. This requires Goals 1/3/5 builders to be **reusable `builder(candidate, frozen_inputs)` functions** and the Goal 3 `evidence_snippets` sidecar to be **row-addressable** (O(100) access). Verified against each goal's locks: **no conflicts** — Goal 1 splits parser from writer; Goal 3 is already raw-feature + REF_DATE-frozen; Goal 5 rules are per-candidate logical tests. Single root **`build_manifest.json`** is the sole top-level entry point referencing all sub-manifests + `runtime_report.json` + `submission_sha256`.
**Goal 7 → upstream carry-backs (RESOLVED, folded into Goals 3/4/6):** Goal 4 emits per-criterion `fit_contribution` (normalized `wᵢ·satᵢ`) + semantic `fit_tier ∈ {Elite,Strong,Plausible,Filler}` (calibrated-space thresholds; relevance label, not gating) + threads `evidence_id`/`source_tier`/`surface_snippet`/`jd_label` on the evidence trail; **Goal 3** adds an `evidence_snippets` sidecar (criterion-linked spans + stable `evidence_id`s, overriding D1's read-on-demand for citations); **Goal 6** consumes `fit_tier`, with `filler_flag ≡ fit_tier==Filler`. No conflicts; tier↔rank divergence bounded and on the Stage-4 watch-list.
**Still open:** numeric tuning executed alongside the build (Goal 4 D9): the importance ladder, θ_assess, dense-hit cutoff, soft-AND ε, attenuation floor, risk weights/floor, engagement slope, and the diagnostic stuffer threshold. The offline eval/self-labeling harness is **decided** as Goal 4 D9 (audit-driven optimization + a ~250–400 hand-labeled tripwire used as a regression alarm only). (Goal 6's `hard_gate_flag` derivation and the twin-safeguard question are now resolved — see Goal 6 D1/D5.)
**Recommended order:** build (Goals 1→3 offline artifacts → Goal 4/5 scoring+risk → Goal 6 selection → Goal 7 reasoning) + the D9 audit/tuning loop.

> **Goal 2 → Goal 3 carry-overs (read before Goal 3 chat):** (1) Embedding model is **locked**: `bge-small-en-v1.5`, 384-d, L2-normalized, with a query/passage-prefix convention — candidate side must use the *passage* prefix to match the JD's *query* prefix. (2) Candidate BM25 doc and dense text must come from **narrative fields (summary, headline, career titles+descriptions), not the `skills[]` array** (diluted ~12k×/skill). (3) Goal 3 features must supply evidence for the Goal 2 criteria's `evidence_sources` (incl. `skill_assessment_scores` for corroboration, derived `applied_ml_years_at_product`, tenure/title-chaser stats, summed-tenure-vs-YOE for honeypot input). (4) `redrob_signals` feed the atomic engagement criteria. (5) Profiling script lives in session scratch — re-runnable; key distributions captured in the Goal 2 section above.

> **Note on Goal 1 ↔ Goal 3 boundary:** Goal 1 is now scoped to **pure ingestion only** (faithful nested Parquet of raw data). All derivation/representation — embeddings, numeric feature matrix, lexical doc — lives in **Goal 3** and is built by iterating Goal 1's canonical table in source row order.
