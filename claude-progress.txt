REDROB RANKING — BUILD PROGRESS
================================
(Update every session. DONE / NEXT / KEY DECISIONS / RISKS / DEVIATIONS.)

=========================================================================
FINAL STATE: COMPLETE. STOP CONDITION MET.
=========================================================================
- 57/57 features pass; 85 tests green.
- FULL 100k precompute done (resumable streaming/memmap build; finalized 100000 rows, F=132).
- `python rank.py --candidates candidates.jsonl --out submission.csv` on the FULL pool:
  "Submission is valid." in 2.08s / 601MB (<=5min, <=16GB, CPU-only, no-network, <=5GB disk).
- Determinism: byte-identical across two full-pool runs.
- Honeypot exposure in top-100: 0/100 (DQ threshold 10%). 0 hard-gated selected.
- Traps: tier5 ranked up, stuffer/honeypots excluded, active>inactive (sample suite + full audit).
- D9 tuning applied: fixed top-100 fit saturation (was 93/100 at fit=1.0 -> now spreads
  0.70-0.99) so NDCG@10 ordering reflects fit quality, not just engagement. Top-10 are genuine
  AI/NLP/Search engineers at product cos with cited assessment corroboration.
- Two-command repro, sandbox (frozen-stats), Dockerfile, README, submission_metadata.yaml shipped.
- build_manifest.json sole root; runtime_report.json; per-goal sub-manifests.
TO PRODUCE THE SUBMISSION FROM SCRATCH:
  python precompute.py --candidates candidates.jsonl --artifacts artifacts --expected-n 100000
  python rank.py --candidates candidates.jsonl --out submission.csv   # then rename to <participant_id>.csv
Before upload: fill TODO fields in submission_metadata.yaml; host the sandbox or use the
README docker run recipe.
=========================================================================

(historical) PHASE 1. ALL 8 GOALS DONE. 57/57 features pass; 85 tests green on sample.
rank.py: 0.13s/62MB, byte-identical, no neural model. Two-command repro proven. Sandbox +
packaging done. Resumable streaming/memmap full-pool builder (byte-identical parity w/ in-memory).
Full ingest validated at scale (100000 rows, 0 quarantined, 13s).
D9 audit (sample): top-50 clean (real ML titles, Elite/Strong, 0 honeypots, ~Tier-1 corroboration);
suspicious titles only in the filler tail (51-100), honestly marked (sample has ~66 relevant of
1510; full pool richer). tiers: 25 Elite/20 Strong/21 Plausible/34 Filler.

FULL 100k PRECOMPUTE: launched in background (RAM freed to 4GB). Resumable (checkpoint per chunk).
When it finishes: run `python rank.py --candidates candidates.jsonl --out submission.csv` and
validate. If interrupted, re-run precompute (resumes from checkpoint).

(historical) Goals 1-6 DONE. END-TO-END SUBMISSION VALID on sample
(validate_submission.py: "Submission is valid."). Next: Goal 7 reasoning (replace placeholder),
then Goal 8 runtime/repro/packaging, then D9 tuning + full 100k run.

MILESTONE: src/select.py produces submission.csv passing the official validator. 100 rows,
0 honeypots in top-100, tier5 IN / stuffer OUT, scores non-increasing, candidate_id tie-break.
41/57 features pass; 70 tests green. Placeholder reasoning in use (Goal 7 replaces).
TODO cosmetic: snippet anchor prints float32 yoe (6.300000190734...) -> fix in snippets.py +
clean number formatting in Goal 7; will rebuild artifacts once (after embedding-throughput fix).

DONE (Goal 2) - jd/jd_query.json (28 typed criteria), jd_ideal.npy, jd_antiprofile.npy [5],
  jd_manifest. src/jd_build.py (validation: evidence paths resolve, enums, unique ids).
  src/embed.py fastembed ONNX bge-small-en-v1.5 (deterministic 0.0 re-embed). F2.* pass.
DONE (Goal 3) - reference/company_table.json (63 cos: 11 services / 8 fictional / 44 product),
  reference/concept_registry.json (9 concepts -> assessment skills + keywords).
  src/represent.py (132 features, NaN/_present by construction), src/build.py (2 embeddings +
  query-aware evidence doc + archetype cosines + role-relevance; batch driver + sandbox builder
  with 0.0 parity), src/bm25.py (narrative-only, skills excluded, phrase-join, precomputed W),
  src/snippets.py (row-addressable sidecar). src/transforms.py (quantile/tail-anchor/trapezoid).
  Sample artifacts built (1510 rows, F=132, REF_DATE=2026-05-28). F3.* pass. 49 tests green.
  KEY trap signal verified: tier5 evidence_cos_ideal=0.90 vs stuffer 0.53; stuffer BM25=0.0.

PERF RISK (must fix before full 100k run, DEFERRED): embedding throughput ~17 docs/s
  (threads=8); sample (8.3k docs) = 460s -> full 100k (~600k docs) ~8.5h. Larger batch is
  SLOWER (onnx). Fix path: fastembed parallel= multiprocessing (data-parallel across cores,
  workers threads=1) + memmap-streaming write + checkpoint/resume. Target <2h. Sample rebuilds
  are a one-time cost; Goal 4-7 just reload artifacts (fast), so this does NOT block the build.

DONE
- Read full spec: Architecture/ (overview, contracts, runtime, artifacts, Goal1-8),
  submission_spec.docx, job_description.docx, candidate_schema.json, validate_submission.py.
- Verified environment (see RISKS).
- Scaffolding: dirs (src/ tests/ artifacts/ data/sample/ scripts/), feature_list.json (~45
  features), .gitignore, requirements.runtime.txt (pinned lean), requirements.precompute.txt,
  this file.

NEXT (PHASE 0 finish, then PHASE 1)
1. init.sh smoke test + git init + initial commit.
2. tests/ harness EARLY (trap fixtures, invariant asserts, budget check) — source of truth.
3. src/common.py (determinism setup, REF_DATE handling, stable hashing, schema constants).
4. Goal 1 ingest (parse_record + streaming parquet writer + candidate_ids + manifest).
5. make_sample.py -> stratified 1-2k sample (incl. honeypots/stuffers/Tier-5/consulting/cv-speech).
6. Stand up the thin precompute->rank->validate skeleton ASAP; deepen Goal-by-Goal (2->8),
   then Goal 4 D9 audit/tuning. Validate on the sample throughout; defer full 100k embed.

KEY DECISIONS
- python (not python3). Build/validate on a 1-2k representative sample; full 100k embed
  deferred to the end (resumable background job) — per user direction.
- Precompute is streaming + memmap + small-batch + CHECKPOINTED/RESUMABLE (RAM-tight machine).
- Embedding stack: prefer lean ONNX (fastembed) for bge-small-en-v1.5; fallback
  sentence-transformers+torch. Same lib both sides (JD + candidates). Pin once verified.
- Competition Runtime deps: numpy/scipy/pyarrow/pandas/joblib only. rank.py loads NO neural model.
- Tie-break: printed = final_score - rank*delta; ONLY equal-printed-score tie-break =
  candidate_id ascending (validate_submission.py ground truth). Never a post-score reorder.
- Sandbox §10.5: docker run recipe (untestable locally — no Docker) + a testable
  sandbox_rank.py on <=100 sample.

RISKS
- RAM ~2 GB free of 16 GB (host 87% used). Ranking step fine (~1.3 GB). Offline embed is the
  pressure point -> frugal+checkpointed; full run deferred.
- No transformers/sentence-transformers/fastembed installed; no bge cached. Precompute needs a
  one-time network fetch (allowed; no-network binds only the ranking step). Verify network at build.
- No Docker locally -> Dockerfile shipped but unverified here; sandbox_rank.py is the tested path.

DONE (Goal 1)
- src/ingest.py: pure parse_record (Sandbox parity) + streaming Zstd nested-Parquet writer +
  candidate_ids.npy + ingest_manifest.json + quarantine-not-drop. read_parquet_records helper.
- Verified on real data: 3000 rows in 2.68s, +75MB RSS; full-100k extrapolates ~90s/~12MB parquet.
  Round-trip lossless modulo documented float32 type choice. Sample (1510 rows) ingests clean.
- scripts/make_sample.py: deterministic stratified 1510-row sample (50 honeypot/300 keyword/
  80 consulting/1070 bg + 10 fixtures) at data/sample/sample.jsonl.
- Features F1.1-F1.6 passing. 20 tests green.

DEVIATIONS FROM PLAN (log each with one-line rationale)
- Goal 1 D2 dictionary-encoding of low-card enums collapsed to plain UTF-8 strings: identical
  round-trip, avoids cross-batch dict-unification pitfalls in nested structs, storage trivial.
- Goal 3 D2 per-role embeddings DROPPED; evidence-doc description ordering switched from dense
  role-relevance to a lexical concept-hit proxy. Rationale: role_relevance (G9) is unused by
  scoring; the evidence doc is identical for the ~95% of candidates that fit 512 tokens; cuts
  embedding work ~3x (2 docs/candidate vs ~6). Architecture stays faithful (identity+evidence
  two-vector). G9 columns retained, filled with the evidence_cos_ideal proxy.
