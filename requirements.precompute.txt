# Precompute + Sandbox dependency set (UNBUDGETED offline phase).
# May carry neural-model / embedding deps. Pinned for reproducibility. Python 3.10.
# Includes the lean runtime set so precompute can also load/verify runtime artifacts.
numpy==2.2.6
scipy==1.15.3
pyarrow==22.0.0
pandas==2.3.3
joblib==1.5.1
PyYAML==6.0.3

# Embedding backend for bge-small-en-v1.5 (384-d, L2-norm, query/passage prefix).
# Selected: fastembed (ONNX Runtime, CPU, no torch at inference) — lean + low-memory +
# deterministic (verified 0.0 re-embed diff, single-thread). Uses the int8-quantized ONNX
# build of bge-small-en-v1.5 (still 384-d/L2-norm; quantization is immaterial for ranking).
# Used ONLY in precompute/sandbox; rank.py never imports it (Runtime ML Policy).
fastembed==0.8.0
onnxruntime==1.23.2
hf_xet==1.5.1
huggingface_hub==0.36.2
